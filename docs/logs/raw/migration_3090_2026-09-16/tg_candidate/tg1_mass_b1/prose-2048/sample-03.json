{
 "started_utc": "2026-09-17T00:05:30.462278+00:00",
 "wall_seconds": 19.101608651995775,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T00:05:49.564146+00:00",
 "warmup": false,
 "sample_index": 3,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 67,
  "temperature_c": {
   "min": 71.0,
   "max": 76.0,
   "mean": 74.53731343283582
  },
  "power_w": {
   "min": 193.68,
   "max": 267.58,
   "mean": 242.59910447761195
  },
  "util_pct": {
   "min": 80.0,
   "max": 100.0,
   "mean": 88.32835820895522
  },
  "clock_sm_mhz": {
   "min": 1620.0,
   "max": 1920.0,
   "mean": 1901.641791044776
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2540.0
  }
 },
 "output_text_sha256": "c325fd049c685fe3213e3213dd2d7666564173daf9723ccedccda59911ea4e87",
 "output_text": ".py.py --.py --url.py --url http.py --url http://.py --url http://1.py --url http://12.py --url http://127.py --url http://127..py --url http://127.0.py --url http://127.0..py --url http://127.0.0.py --url http://127.0.0..py --url http://127.0.0.1.py --url http://127.0.0.1:.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30.py --url http://127.0.0.1:300.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 .py --url http://127.0.0.1:30000 --prompt-tokens 101 4.py --url http://127.0.0.1:30000 --prompt-tokens 101 42.py --url http://127.0.0.1:30000 --prompt-tokens 101 421.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 17.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 170.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 68.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 682.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 100.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1000.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nS.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sg.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.s.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.q.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 5.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at pre.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill)..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in S.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sg.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.s.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.q.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsg.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (q.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-7.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 Mo.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gem.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n           .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n           .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_ple_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_ple_tune.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_ple_tune1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_ple_tune10.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_ple_tune10.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_ple_tune10.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_ple_tune10.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_ple_tune10.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_ple_tune10.py, ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_ple_tune10.py, ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_ple_tune10.py, ngram_ple_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_ple_tune10.py, ngram_ple_tune.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_ple_tune10.py, ngram_ple_tune1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_ple_tune10.py, ngram_ple_tune11.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_ple_tune10.py, ngram_ple_tune11.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_ple_tune10.py, ngram_ple_tune11.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_ple_tune10.py, ngram_ple_tune11.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_ple_tune10.py, ngram_ple_tune11.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_ple_tune10.py, ngram_ple_tune11.py, ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_ple_tune10.py, ngram_ple_tune11.py, ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_ple_tune10.py, ngram_ple_tune11.py, ngram_ple_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_ple_tune10.py, ngram_ple_tune11.py, ngram_ple_tune.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_ple_tune10.py, ngram_ple_tune11.py, ngram_ple_tune1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_ple_tune10.py, ngram_ple_tune11.py, ngram_ple_tune12.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_ple_tune10.py, ngram_ple_tune11.py, ngram_ple_tune12.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_ple_tune10.py, ngram_ple_tune11.py, ngram_ple_tune12.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_ple_tune10.py, ngram_ple_tune11.py, ngram_ple_tune12.py,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_ple_tune10.py, ngram_ple_tune11.py, ngram_ple_tune12.py,\n           .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_ple_tune10.py, ngram_ple_tune11.py, ngram_ple_tune12.py,\n            n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_ple_tune10.py, ngram_ple_tune11.py, ngram_ple_tune12.py,\n            ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_ple_tune10.py, ngram_ple_tune11.py, ngram_ple_tune12.py,\n            ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_ple_tune10.py, ngram_ple_tune11.py, ngram_ple_tune12.py,\n            ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_ple_tune10.py, ngram_ple_tune11.py, ngram_ple_tune12.py,\n            ngram_ple_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_ple_tune10.py, ngram_ple_tune11.py, ngram_ple_tune12.py,\n            ngram_ple_tune.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_ple_tune10.py, ngram_ple_tune11.py, ngram_ple_tune12.py,\n            ngram_ple_tune1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_ple_tune10.py, ngram_ple_tune11.py, ngram_ple_tune12.py,\n            ngram_ple_tune13.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_ple_tune10.py, ngram_ple_tune11.py, ngram_ple_tune12.py,\n            ngram_ple_tune13.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_ple_tune10.py, ngram_ple_tune11.py, ngram_ple_tune12.py,\n            ngram_ple_tune13.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_ple_tune10.py, ngram_ple_tune11.py, ngram_ple_tune12.py,\n            ngram_ple_tune13.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_ple_tune10.py, ngram_ple_tune11.py, ngram_ple_tune12.py,\n            ngram_ple_tune13.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_ple_tune10.py, ngram_ple_tune11.py, ngram_ple_tune12.py,\n            ngram_ple_tune13.py, ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_ple_tune10.py, ngram_ple_tune11.py, ngram_ple_tune12.py,\n            ngram_ple_tune13.py, ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_ple_tune10.py, ngram_ple_tune11.py, ngram_ple_tune12.py,\n            ngram_ple_tune13.py, ngram_ple_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_ple_tune10.py, ngram_ple_tune11.py, ngram_ple_tune12.py,\n            ngram_ple_tune13.py, ngram_ple_tune.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_ple_tune10.py, ngram_ple_tune11.py, ngram_ple_tune12.py,\n            ngram_ple_tune13.py, ngram_ple_tune1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_ple_tune10.py, ngram_ple_tune11.py, ngram_ple_tune12.py,\n            ngram_ple_tune13.py, ngram_ple_tune14.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, launches\nSGLang with the patched `sglang.srt.model_loader.qwen4exp` model class and waits for `/health`. The\npublished configuration is `--max-running-requests 1` (one request at a time), `--enable-ple` and\n`--ple-ngram-path ~/quant/model/ple` (the PLE n-gram table, 51.2 GB, read from NVMe at prefill). The\n`--ple-ngram-path` argument is part of the patch. The `--model-class` argument is not in SGLang at the base\ncommit: the patch adds it, and `serve.sh` passes `sglang.srt.model_loader.qwen4exp` through it.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV read/write, PLE gather, int4 KV read/write\nscripts/        serve.sh (the launcher), the patch build chain, the elastic-control daemon, the KV-tier probe\ntools/        the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_tune.py, ngram_ple_tune2.py,\n            ngram_ple_tune3.py, ngram_ple_tune4.py, ngram_ple_tune5.py, ngram_ple_tune6.py, ngram_ple_tune7.py,\n            ngram_ple_tune8.py, ngram_ple_tune9.py, ngram_ple_tune10.py, ngram_ple_tune11.py, ngram_ple_tune12.py,\n            ngram_ple_tune13.py, ngram_ple_tune14.py",
 "ttft_seconds": 1.3304869970015716,
 "status": "ok",
 "tg_tokens_per_s": 28.754803326998644,
 "decode_seconds_per_token": 0.03477679845791446,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 31.54504999110941,
  "p95": 51.34522099979222,
  "mean": 34.77679845791446
 },
 "class": "prose",
 "requested_input_tokens": 2048
}
