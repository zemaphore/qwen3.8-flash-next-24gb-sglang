{
 "started_utc": "2026-09-17T00:04:34.676733+00:00",
 "wall_seconds": 16.734184547007317,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T00:04:51.411039+00:00",
 "warmup": true,
 "sample_index": 0,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 60,
  "temperature_c": {
   "min": 65.0,
   "max": 73.0,
   "mean": 71.1
  },
  "power_w": {
   "min": 120.25,
   "max": 271.4,
   "mean": 244.28083333333333
  },
  "util_pct": {
   "min": 0.0,
   "max": 96.0,
   "mean": 83.01666666666667
  },
  "clock_sm_mhz": {
   "min": 1785.0,
   "max": 1935.0,
   "mean": 1924.25
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2540.0
  }
 },
 "output_text_sha256": "086e8732ee34b676f9c2d3c2e1ff423301fa0965a33cd72efd3ce61e78a815fb",
 "output_text": ".py.py --.py --url.py --url http.py --url http://.py --url http://1.py --url http://12.py --url http://127.py --url http://127..py --url http://127.0.py --url http://127.0..py --url http://127.0.0.py --url http://127.0.0..py --url http://127.0.0.1.py --url http://127.0.0.1:.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30.py --url http://127.0.0.1:300.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --re.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/b.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:300.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 100.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 1000.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --re.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/b.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `pref.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run;.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warm.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsg.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (q.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-7.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nk.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 Mo.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gem.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gem.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/         .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, v.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, v.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, v.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, v.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloor.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script),.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_el.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_at.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/          .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log),.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents),.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state),.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n###.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sg.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/q.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-7.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a255206f.patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a255206f.patch`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a255206f.patch` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch: .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch: 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch: 15.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch: 15 files.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch: 15 files,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch: 15 files, .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch: 15 files, 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch: 15 files, 1,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch: 15 files, 1,0.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch: 15 files, 1,04.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch: 15 files, 1,040.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch: 15 files, 1,040 added.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch: 15 files, 1,040 added lines.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch: 15 files, 1,040 added lines,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch: 15 files, 1,040 added lines, .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch: 15 files, 1,040 added lines, 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch: 15 files, 1,040 added lines, 13.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch: 15 files, 1,040 added lines, 13 deleted.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch: 15 files, 1,040 added lines, 13 deleted,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch: 15 files, 1,040 added lines, 13 deleted,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch: 15 files, 1,040 added lines, 13 deleted,\nagainst.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch: 15 files, 1,040 added lines, 13 deleted,\nagainst S.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch: 15 files, 1,040 added lines, 13 deleted,\nagainst SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch: 15 files, 1,040 added lines, 13 deleted,\nagainst SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch: 15 files, 1,040 added lines, 13 deleted,\nagainst SGLang at.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch: 15 files, 1,040 added lines, 13 deleted,\nagainst SGLang at `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch: 15 files, 1,040 added lines, 13 deleted,\nagainst SGLang at `7.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch: 15 files, 1,040 added lines, 13 deleted,\nagainst SGLang at `73.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch: 15 files, 1,040 added lines, 13 deleted,\nagainst SGLang at `73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch: 15 files, 1,040 added lines, 13 deleted,\nagainst SGLang at `73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch: 15 files, 1,040 added lines, 13 deleted,\nagainst SGLang at `73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch: 15 files, 1,040 added lines, 13 deleted,\nagainst SGLang at `73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch: 15 files, 1,040 added lines, 13 deleted,\nagainst SGLang at `73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch: 15 files, 1,040 added lines, 13 deleted,\nagainst SGLang at `73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch: 15 files, 1,040 added lines, 13 deleted,\nagainst SGLang at `73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 101 --repeats 3\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints the launch banner and the accepted decode numbers when the server comes up.\n`tools/bench.py` prints `decode tok/s` and `prefill tok/s` per run; `tools/ple.py` prints the PLE\nn-gram table's hit rate. The server's first request is a 101-token warmup, so the first real request\nis already warm.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-cache tier module, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV kernels, PLE kernels, the sm_120 tuner\ntools/          the measurement tools: bench, ple, ngram, nll, vram, vramcheck, vramfloor, vramfloorcheck\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant), build_flash_attn.sh\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the measurement logs)\nassets/         phase1_state.json (the accepted state), the two serving assets, the tuner's configs\n```\n\n## What is in the repository\n\n### The serving patch and the layers it was built from\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch: 15 files, 1,040 added lines, 13 deleted,\nagainst SGLang at `73a255206f",
 "ttft_seconds": 2.8502430820080917,
 "status": "ok",
 "tg_tokens_per_s": 36.80528887961135,
 "decode_seconds_per_token": 0.027170008182002336,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 27.20032600336708,
  "p95": 33.56370600522496,
  "mean": 27.170008182002334
 },
 "class": "prose",
 "requested_input_tokens": 2048
}
