{
 "started_utc": "2026-09-17T00:55:38.829780+00:00",
 "wall_seconds": 15.016012620995753,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T00:55:53.845852+00:00",
 "warmup": false,
 "sample_index": 1,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 54,
  "temperature_c": {
   "min": 72.0,
   "max": 75.0,
   "mean": 73.42592592592592
  },
  "power_w": {
   "min": 243.86,
   "max": 270.74,
   "mean": 260.8933333333333
  },
  "util_pct": {
   "min": 88.0,
   "max": 97.0,
   "mean": 90.68518518518519
  },
  "clock_sm_mhz": {
   "min": 1665.0,
   "max": 1920.0,
   "mean": 1900.5555555555557
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2540.0
  }
 },
 "output_text_sha256": "3e87d9bc7915f6244f96d8603849a40813131466babce4f06261966dd0ab1cbd",
 "output_text": ".py.py --.py --url.py --url http.py --url http://.py --url http://1.py --url http://12.py --url http://127.py --url http://127..py --url http://127.0.py --url http://127.0..py --url http://127.0.0.py --url http://127.0.0..py --url http://127.0.0.1.py --url http://127.0.0.1:.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30.py --url http://127.0.0.1:300.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 .py --url http://127.0.0.1:30000 --prompt-tokens 101 4.py --url http://127.0.0.1:30000 --prompt-tokens 101 42.py --url http://127.0.0.1:30000 --prompt-tokens 101 421.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 17.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 170.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 68.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 682.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 100.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1000.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches S.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`;.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 26.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,14.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:44.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448)..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` +.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POP.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:44.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448)..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsg.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (q.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-7.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the S.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert G.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quant.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (through.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n             .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (N.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (N.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (N.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/      .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,0.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,05.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at S.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `7.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Q.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint2/m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint2/mixed.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint2/mixed INT.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint2/mixed INT8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint2/mixed INT8 weight.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint2/mixed INT8 weight path.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint2/mixed INT8 weight path,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint2/mixed INT8 weight path, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint2/mixed INT8 weight path, the tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint2/mixed INT8 weight path, the tiered.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint2/mixed INT8 weight path, the tiered KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint2/mixed INT8 weight path, the tiered KV cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint2/mixed INT8 weight path, the tiered KV cache,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint2/mixed INT8 weight path, the tiered KV cache, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint2/mixed INT8 weight path, the tiered KV cache, the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint2/mixed INT8 weight path, the tiered KV cache, the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint2/mixed INT8 weight path, the tiered KV cache, the PLE n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint2/mixed INT8 weight path, the tiered KV cache, the PLE n-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint2/mixed INT8 weight path, the tiered KV cache, the PLE n-gram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint2/mixed INT8 weight path, the tiered KV cache, the PLE n-gram table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint2/mixed INT8 weight path, the tiered KV cache, the PLE n-gram table,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint2/mixed INT8 weight path, the tiered KV cache, the PLE n-gram table, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint2/mixed INT8 weight path, the tiered KV cache, the PLE n-gram table, the elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint2/mixed INT8 weight path, the tiered KV cache, the PLE n-gram table, the elastic expert.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint2/mixed INT8 weight path, the tiered KV cache, the PLE n-gram table, the elastic expert cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint2/mixed INT8 weight path, the tiered KV cache, the PLE n-gram table, the elastic expert cache and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint2/mixed INT8 weight path, the tiered KV cache, the PLE n-gram table, the elastic expert cache and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint2/mixed INT8 weight path, the tiered KV cache, the PLE n-gram table, the elastic expert cache and the\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint2/mixed INT8 weight path, the tiered KV cache, the PLE n-gram table, the elastic expert cache and the\nPLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint2/mixed INT8 weight path, the tiered KV cache, the PLE n-gram table, the elastic expert cache and the\nPLE-aware.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint2/mixed INT8 weight path, the tiered KV cache, the PLE n-gram table, the elastic expert cache and the\nPLE-aware scheduler.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint2/mixed INT8 weight path, the tiered KV cache, the PLE n-gram table, the elastic expert cache and the\nPLE-aware scheduler..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint2/mixed INT8 weight path, the tiered KV cache, the PLE n-gram table, the elastic expert cache and the\nPLE-aware scheduler. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint2/mixed INT8 weight path, the tiered KV cache, the PLE n-gram table, the elastic expert cache and the\nPLE-aware scheduler. The files.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint2/mixed INT8 weight path, the tiered KV cache, the PLE n-gram table, the elastic expert cache and the\nPLE-aware scheduler. The files are.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint2/mixed INT8 weight path, the tiered KV cache, the PLE n-gram table, the elastic expert cache and the\nPLE-aware scheduler. The files are also.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint2/mixed INT8 weight path, the tiered KV cache, the PLE n-gram table, the elastic expert cache and the\nPLE-aware scheduler. The files are also available.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint2/mixed INT8 weight path, the tiered KV cache, the PLE n-gram table, the elastic expert cache and the\nPLE-aware scheduler. The files are also available individually.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint2/mixed INT8 weight path, the tiered KV cache, the PLE n-gram table, the elastic expert cache and the\nPLE-aware scheduler. The files are also available individually under.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint2/mixed INT8 weight path, the tiered KV cache, the PLE n-gram table, the elastic expert cache and the\nPLE-aware scheduler. The files are also available individually under `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint2/mixed INT8 weight path, the tiered KV cache, the PLE n-gram table, the elastic expert cache and the\nPLE-aware scheduler. The files are also available individually under `patches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint2/mixed INT8 weight path, the tiered KV cache, the PLE n-gram table, the elastic expert cache and the\nPLE-aware scheduler. The files are also available individually under `patches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint2/mixed INT8 weight path, the tiered KV cache, the PLE n-gram table, the elastic expert cache and the\nPLE-aware scheduler. The files are also available individually under `patches/`..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint2/mixed INT8 weight path, the tiered KV cache, the PLE n-gram table, the elastic expert cache and the\nPLE-aware scheduler. The files are also available individually under `patches/`.\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint2/mixed INT8 weight path, the tiered KV cache, the PLE n-gram table, the elastic expert cache and the\nPLE-aware scheduler. The files are also available individually under `patches/`.\n\n|.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint2/mixed INT8 weight path, the tiered KV cache, the PLE n-gram table, the elastic expert cache and the\nPLE-aware scheduler. The files are also available individually under `patches/`.\n\n| File.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual files, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, INT4 KV pool, PLE gather)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), the quantization and validation scripts\ntools/        bench.py (throughput), ngram.py (PLE table builder), ngram_quality.py (PLE quality),\n              nll.py (KV-cache quality), nll.py (NLL probe), nll.py (NLL probe), nll.py (NLL probe)\ndocs/         CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (dated logs)\nassets/       phase1_state.json (the accepted state), phase1_state.json (the accepted state)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen4-Exp model class, the\nint2/mixed INT8 weight path, the tiered KV cache, the PLE n-gram table, the elastic expert cache and the\nPLE-aware scheduler. The files are also available individually under `patches/`.\n\n| File |",
 "ttft_seconds": 1.3250429420004366,
 "status": "ok",
 "tg_tokens_per_s": 37.32401801925543,
 "decode_seconds_per_token": 0.02679239945399503,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 26.036052004201338,
  "p95": 36.05999999854248,
  "mean": 26.79239945399503
 },
 "class": "prose",
 "requested_input_tokens": 2048
}
