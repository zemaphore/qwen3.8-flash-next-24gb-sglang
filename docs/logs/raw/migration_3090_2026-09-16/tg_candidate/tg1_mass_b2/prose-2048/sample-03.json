{
 "started_utc": "2026-09-17T00:56:09.212364+00:00",
 "wall_seconds": 15.289287986001,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T00:56:24.501737+00:00",
 "warmup": false,
 "sample_index": 3,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 55,
  "temperature_c": {
   "min": 75.0,
   "max": 76.0,
   "mean": 75.18181818181819
  },
  "power_w": {
   "min": 254.73,
   "max": 276.07,
   "mean": 263.00127272727275
  },
  "util_pct": {
   "min": 89.0,
   "max": 100.0,
   "mean": 91.69090909090909
  },
  "clock_sm_mhz": {
   "min": 1665.0,
   "max": 1920.0,
   "mean": 1908.0
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2540.0
  }
 },
 "output_text_sha256": "57441d33958032bc8e5f708aeda3ee9c0f4977e38ff2d01aea3f0aac405995f2",
 "output_text": ".py.py --.py --url.py --url http.py --url http://.py --url http://1.py --url http://12.py --url http://127.py --url http://127..py --url http://127.0.py --url http://127.0..py --url http://127.0.0.py --url http://127.0.0..py --url http://127.0.0.1.py --url http://127.0.0.1:.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30.py --url http://127.0.0.1:300.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:300.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches S.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`;.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 26.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,14.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:44.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448)..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional;.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional;.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`);.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-g.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:29.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290);.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-d.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-g.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NV.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:29.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-g.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:29.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-d.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-g.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NV.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:29.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-g.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:29.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290);.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-d.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-g.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NV.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging\nfile.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging\nfile,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging\nfile, CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging\nfile, CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging\nfile, CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging\nfile, CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging\nfile, CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging\nfile, CAMPAIGN.md:2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging\nfile, CAMPAIGN.md:29.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging\nfile, CAMPAIGN.md:290.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging\nfile, CAMPAIGN.md:290);.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging\nfile, CAMPAIGN.md:290); `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging\nfile, CAMPAIGN.md:290); `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging\nfile, CAMPAIGN.md:290); `--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging\nfile, CAMPAIGN.md:290); `--ple-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging\nfile, CAMPAIGN.md:290); `--ple-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging\nfile, CAMPAIGN.md:290); `--ple-ple-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging\nfile, CAMPAIGN.md:290); `--ple-ple-ram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging\nfile, CAMPAIGN.md:290); `--ple-ple-ram-g.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging\nfile, CAMPAIGN.md:290); `--ple-ple-ram-gib.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging\nfile, CAMPAIGN.md:290); `--ple-ple-ram-gib`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging\nfile, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging\nfile, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging\nfile, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging\nfile, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging\nfile, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging\nfile, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24 (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging\nfile, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24 (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging\nfile, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24 (the host.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging\nfile, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24 (the host RAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging\nfile, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24 (the host RAM the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging\nfile, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging\nfile, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging\nfile, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging\nfile, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging\nfile, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging\nfile, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging\nfile, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging\nfile, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging\nfile, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging\nfile, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging\nfile, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging\nfile, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging\nfile, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:29.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging\nfile, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-*` flags are optional; the defaults are the tuned values. The `--ple-*` flags\nare optional; the defaults are the tuned values. `--ple-ple-dir` defaults to `$PLE` (the directory holding\n`ple.json` and `ple.f8_e4m3.bin`); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290);\n`--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into, CAMPAIGN.md:290);\n`--ple-ple-disk-gib` defaults to 24 (the NVMe staging file, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24\n(the host RAM the table is staged into, CAMPAIGN.md:290); `--ple-ple-disk-gib` defaults to 24 (the NVMe staging\nfile, CAMPAIGN.md:290); `--ple-ple-ram-gib` defaults to 24 (the host RAM the table is staged into,\nCAMPAIGN.md:290);",
 "ttft_seconds": 1.0486300400079926,
 "status": "ok",
 "tg_tokens_per_s": 35.88339212592063,
 "decode_seconds_per_token": 0.02786804537572251,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 27.3669379967032,
  "p95": 36.16888799297158,
  "mean": 27.86804537572251
 },
 "class": "prose",
 "requested_input_tokens": 2048
}
