{
 "started_utc": "2026-09-17T00:55:22.211015+00:00",
 "wall_seconds": 16.6168568800058,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T00:55:38.827941+00:00",
 "warmup": true,
 "sample_index": 0,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 60,
  "temperature_c": {
   "min": 65.0,
   "max": 73.0,
   "mean": 71.05
  },
  "power_w": {
   "min": 127.77,
   "max": 267.68,
   "mean": 245.178
  },
  "util_pct": {
   "min": 0.0,
   "max": 95.0,
   "mean": 82.75
  },
  "clock_sm_mhz": {
   "min": 1740.0,
   "max": 1935.0,
   "mean": 1917.5
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2540.0
  }
 },
 "output_text_sha256": "e9fa95932150e0566fe05e446d0f193f8cc6e9b36e5ad87eefcddb06a4b776b5",
 "output_text": ".py.py --.py --url.py --url http.py --url http://.py --url http://1.py --url http://12.py --url http://127.py --url http://127..py --url http://127.0.py --url http://127.0..py --url http://127.0.0.py --url http://127.0.0..py --url http://127.0.0.1.py --url http://127.0.0.1:.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30.py --url http://127.0.0.1:300.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/b.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:300.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 100.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 1000.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 100.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 1000.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 100.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 1000.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 100.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 1000.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=....py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use;.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warm.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:41.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413)..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsg.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (q.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-7.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nk.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_m.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert G.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEM.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM),.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_m.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n               .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_m.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test),.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_m.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script),.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_el.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n               .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant),.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant),.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n               .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                un.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff),.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.b.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/p.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/pref.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput),.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe),.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n               .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder),.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test),.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype),.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), ngram_poc.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), ngram_poc_test.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), ngram_poc_test.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), ngram_poc_test.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), ngram_poc_test.py,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), ngram_poc_test.py,\n               .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), ngram_poc_test.py,\n                n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), ngram_poc_test.py,\n                ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), ngram_poc_test.py,\n                ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), ngram_poc_test.py,\n                ngram_poc.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), ngram_poc_test.py,\n                ngram_poc_test.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), ngram_poc_test.py,\n                ngram_poc_test2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), ngram_poc_test.py,\n                ngram_poc_test2.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), ngram_poc_test.py,\n                ngram_poc_test2.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), ngram_poc_test.py,\n                ngram_poc_test2.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), ngram_poc_test.py,\n                ngram_poc_test2.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), ngram_poc_test.py,\n                ngram_poc_test2.py, ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), ngram_poc_test.py,\n                ngram_poc_test2.py, ngram_poc.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), ngram_poc_test.py,\n                ngram_poc_test2.py, ngram_poc_test.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), ngram_poc_test.py,\n                ngram_poc_test2.py, ngram_poc_test3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), ngram_poc_test.py,\n                ngram_poc_test2.py, ngram_poc_test3.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), ngram_poc_test.py,\n                ngram_poc_test2.py, ngram_poc_test3.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), ngram_poc_test.py,\n                ngram_poc_test2.py, ngram_poc_test3.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), ngram_poc_test.py,\n                ngram_poc_test2.py, ngram_poc_test3.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), ngram_poc_test.py,\n                ngram_poc_test2.py, ngram_poc_test3.py, ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), ngram_poc_test.py,\n                ngram_poc_test2.py, ngram_poc_test3.py, ngram_poc.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), ngram_poc_test.py,\n                ngram_poc_test2.py, ngram_poc_test3.py, ngram_poc_test.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), ngram_poc_test.py,\n                ngram_poc_test2.py, ngram_poc_test3.py, ngram_poc_test4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), ngram_poc_test.py,\n                ngram_poc_test2.py, ngram_poc_test3.py, ngram_poc_test4.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), ngram_poc_test.py,\n                ngram_poc_test2.py, ngram_poc_test3.py, ngram_poc_test4.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), ngram_poc_test.py,\n                ngram_poc_test2.py, ngram_poc_test3.py, ngram_poc_test4.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), ngram_poc_test.py,\n                ngram_poc_test2.py, ngram_poc_test3.py, ngram_poc_test4.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), ngram_poc_test.py,\n                ngram_poc_test2.py, ngram_poc_test3.py, ngram_poc_test4.py, ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), ngram_poc_test.py,\n                ngram_poc_test2.py, ngram_poc_test3.py, ngram_poc_test4.py, ngram_poc.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), ngram_poc_test.py,\n                ngram_poc_test2.py, ngram_poc_test3.py, ngram_poc_test4.py, ngram_poc_test.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), ngram_poc_test.py,\n                ngram_poc_test2.py, ngram_poc_test3.py, ngram_poc_test4.py, ngram_poc_test5.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), ngram_poc_test.py,\n                ngram_poc_test2.py, ngram_poc_test3.py, ngram_poc_test4.py, ngram_poc_test5.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), ngram_poc_test.py,\n                ngram_poc_test2.py, ngram_poc_test3.py, ngram_poc_test4.py, ngram_poc_test5.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), ngram_poc_test.py,\n                ngram_poc_test2.py, ngram_poc_test3.py, ngram_poc_test4.py, ngram_poc_test5.py,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), ngram_poc_test.py,\n                ngram_poc_test2.py, ngram_poc_test3.py, ngram_poc_test4.py, ngram_poc_test5.py,\n               .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), ngram_poc_test.py,\n                ngram_poc_test2.py, ngram_poc_test3.py, ngram_poc_test4.py, ngram_poc_test5.py,\n                n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), ngram_poc_test.py,\n                ngram_poc_test2.py, ngram_poc_test3.py, ngram_poc_test4.py, ngram_poc_test5.py,\n                ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), ngram_poc_test.py,\n                ngram_poc_test2.py, ngram_poc_test3.py, ngram_poc_test4.py, ngram_poc_test5.py,\n                ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), ngram_poc_test.py,\n                ngram_poc_test2.py, ngram_poc_test3.py, ngram_poc_test4.py, ngram_poc_test5.py,\n                ngram_poc.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), ngram_poc_test.py,\n                ngram_poc_test2.py, ngram_poc_test3.py, ngram_poc_test4.py, ngram_poc_test5.py,\n                ngram_poc_test.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 10001 --max-tokens 10001\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10001 --max-tokens 10001\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds longer than the rest because the PLE table is read into host RAM\nat first use; the `tools/ple.py` probe measures that first-request cost. The server's own log records the\nsame event as `ple: warmup done` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (expert GEMM), int2_moe_configs.py (tuned configs),\n                int2_moe_test.py (the kernel test), int2_moe_poc.py (the first prototype)\nscripts/        serve.sh (the launch script), serve_elastic.sh (the elastic-control variant),\n                serve_tiers.sh (the KV-tier variant), serve_ple.sh (the PLE variant), serve.sh.orig (the\n                unpatched launch script, for the diff), serve.sh.bak (the pre-elastic variant)\ntools/        bench.py (decode/prefill throughput), ple.py (the PLE probe), ngram.py (the n-gram table\n                builder), ngram_test.py (its test), ngram_poc.py (its prototype), ngram_poc_test.py,\n                ngram_poc_test2.py, ngram_poc_test3.py, ngram_poc_test4.py, ngram_poc_test5.py,\n                ngram_poc_test6",
 "ttft_seconds": 2.639034014006029,
 "status": "ok",
 "tg_tokens_per_s": 36.55806520891999,
 "decode_seconds_per_token": 0.027353745180037723,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 26.788363989908248,
  "p95": 35.42508500686381,
  "mean": 27.353745180037723
 },
 "class": "prose",
 "requested_input_tokens": 2048
}
