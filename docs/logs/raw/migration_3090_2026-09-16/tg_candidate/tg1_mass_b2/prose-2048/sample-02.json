{
 "started_utc": "2026-09-17T00:55:53.847529+00:00",
 "wall_seconds": 15.363046732993098,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T00:56:09.210655+00:00",
 "warmup": false,
 "sample_index": 2,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 55,
  "temperature_c": {
   "min": 74.0,
   "max": 75.0,
   "mean": 74.41818181818182
  },
  "power_w": {
   "min": 252.23,
   "max": 274.89,
   "mean": 262.46181818181816
  },
  "util_pct": {
   "min": 87.0,
   "max": 100.0,
   "mean": 91.49090909090908
  },
  "clock_sm_mhz": {
   "min": 1680.0,
   "max": 1920.0,
   "mean": 1902.8181818181818
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2540.0
  }
 },
 "output_text_sha256": "9eed8b39af4457d87c6e72d5c4074296607b2d4c78829411bc2589204dc10e0f",
 "output_text": ".py.py --.py --url.py --url http.py --url http://.py --url http://1.py --url http://12.py --url http://127.py --url http://127..py --url http://127.0.py --url http://127.0..py --url http://127.0.0.py --url http://127.0.0..py --url http://127.0.0.1.py --url http://127.0.0.1:.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30.py --url http://127.0.0.1:300.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:300.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches S.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`;.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 26.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,14.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:41.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413)..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sg.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/s.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:11.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`),.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tp.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sg.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/s.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`);.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NV.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`m.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` +.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POP.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` +.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `M.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_W.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILL.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sg.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/s.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:11.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-14.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`);.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsg.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (q.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-7.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_m.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_m.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n               .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_m.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.c.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_m.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_m.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.c.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_m.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_m.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.c.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu,\n               .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu,\n                int.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu,\n                int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu,\n                int2_m.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu,\n                int2_moe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu,\n                int2_moe_cuda.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu,\n                int2_moe_cuda.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu,\n                int2_moe_cuda.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu,\n                int2_moe_cuda.py, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu,\n                int2_moe_cuda.py, int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu,\n                int2_moe_cuda.py, int2_m.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu,\n                int2_moe_cuda.py, int2_moe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu,\n                int2_moe_cuda.py, int2_moe_cuda.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu,\n                int2_moe_cuda.py, int2_moe_cuda.c.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu,\n                int2_moe_cuda.py, int2_moe_cuda.cu.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu,\n                int2_moe_cuda.py, int2_moe_cuda.cu,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu,\n                int2_moe_cuda.py, int2_moe_cuda.cu, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu,\n                int2_moe_cuda.py, int2_moe_cuda.cu, int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu,\n                int2_moe_cuda.py, int2_moe_cuda.cu, int2_m.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu,\n                int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu,\n                int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu,\n                int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu,\n                int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu,\n                int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu,\n                int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu,\n                int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_m.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu,\n                int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu,\n                int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu,\n                int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.c.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu,\n                int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu,\n                int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu,\n                int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu,\n                int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu,\n                int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_m.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu,\n                int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu,\n                int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the 262,144-token context (CAMPAIGN.md:413).\n`--enable-ple` alone is not enough: `PLE_DIR` must point at a directory holding `ple.json` and `ple.f8_e4m3.bin`\n(the PLE table is loaded from `ple.json`'s `ple_table` path, `sglang/srt/model_loader/ple_loader.py:115`), and\n`--ple-tpb` must be set explicitly because the checkpoint's `ple_config` does not carry it\n(`sglang/srt/model_config.py:1011`); `scripts/serve.sh` sets both. The table lives on NVMe and is served through\n`mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (`sglang/srt/model_loader/ple_loader.py:115-141`); `PLE_DIR` is the\ndirectory, not the file.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, ple.py, int2_moe.py, int2_moe_cuda.py,\n                int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu,\n                int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py, int2_moe_cuda.cu, int2_moe_cuda.py",
 "ttft_seconds": 1.1118379239924252,
 "status": "ok",
 "tg_tokens_per_s": 35.856745556768836,
 "decode_seconds_per_token": 0.02788875522506045,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 27.789226995082572,
  "p95": 35.13590000511613,
  "mean": 27.88875522506045
 },
 "class": "prose",
 "requested_input_tokens": 2048
}
