{
 "started_utc": "2026-09-17T00:57:48.022109+00:00",
 "wall_seconds": 19.75830168298853,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T00:58:07.780607+00:00",
 "warmup": false,
 "sample_index": 1,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 71,
  "temperature_c": {
   "min": 76.0,
   "max": 77.0,
   "mean": 76.1830985915493
  },
  "power_w": {
   "min": 230.49,
   "max": 270.24,
   "mean": 245.00338028169017
  },
  "util_pct": {
   "min": 91.0,
   "max": 100.0,
   "mean": 93.2394366197183
  },
  "clock_sm_mhz": {
   "min": 1620.0,
   "max": 1920.0,
   "mean": 1906.056338028169
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2540.0
  }
 },
 "output_text_sha256": "fde18e530620fbe1257f2fa18cae0fff12c1c651e18ef48e5d688cdd1e6fc56e",
 "output_text": "++       +        b+        b_z+        b_zp+        b_zp_num+        b_zp_num =+        b_zp_num = +        b_zp_num = 1+        b_zp_num = 12+        b_zp_num = 128+        b_zp_num = 128\n+        b_zp_num = 128\n++        b_zp_num = 128\n+   +        b_zp_num = 128\n+    elif+        b_zp_num = 128\n+    elif has+        b_zp_num = 128\n+    elif has_z+        b_zp_num = 128\n+    elif has_zp+        b_zp_num = 128\n+    elif has_zp and+        b_zp_num = 128\n+    elif has_zp and use+        b_zp_num = 128\n+    elif has_zp and use_int+        b_zp_num = 128\n+    elif has_zp and use_int2+        b_zp_num = 128\n+    elif has_zp and use_int2_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_sh+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_exp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr +=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_exp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N),+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float3+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUT+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M %+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95ee+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(top+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540e+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201c+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95ee+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201c+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201c+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_exp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(exp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(t+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int6+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_exp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_exp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts ==+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # ------------------------------------------------+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n+                   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n+                    stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n+                    stride_cm+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n+                    stride_cm,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n+                    stride_cm,\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n+                    stride_cm,\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n+                    stride_cm,\n+                   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n+                    stride_cm,\n+                    stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n+                    stride_cm,\n+                    stride_cn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n+                    stride_cm,\n+                    stride_cn,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n+                    stride_cm,\n+                    stride_cn,\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n+                    stride_cm,\n+                    stride_cn,\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n+                    stride_cm,\n+                    stride_cn,\n+                   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n+                    stride_cm,\n+                    stride_cn,\n+                    pid+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n+                    stride_cm,\n+                    stride_cn,\n+                    pid_n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n+                    stride_cm,\n+                    stride_cn,\n+                    pid_n,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n+                    stride_cm,\n+                    stride_cn,\n+                    pid_n,\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n+                    stride_cm,\n+                    stride_cn,\n+                    pid_n,\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n+                    stride_cm,\n+                    stride_cn,\n+                    pid_n,\n+                   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n+                    stride_cm,\n+                    stride_cn,\n+                    pid_n,\n+                    N+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n+                    stride_cm,\n+                    stride_cn,\n+                    pid_n,\n+                    N,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n+                    stride_cm,\n+                    stride_cn,\n+                    pid_n,\n+                    N,\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n+                    stride_cm,\n+                    stride_cn,\n+                    pid_n,\n+                    N,\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n+                    stride_cm,\n+                    stride_cn,\n+                    pid_n,\n+                    N,\n+                   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n+                    stride_cm,\n+                    stride_cn,\n+                    pid_n,\n+                    N,\n+                    offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n+                    stride_cm,\n+                    stride_cn,\n+                    pid_n,\n+                    N,\n+                    offs_token+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n+                    stride_cm,\n+                    stride_cn,\n+                    pid_n,\n+                    N,\n+                    offs_token,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n+                    stride_cm,\n+                    stride_cn,\n+                    pid_n,\n+                    N,\n+                    offs_token,\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n+                    stride_cm,\n+                    stride_cn,\n+                    pid_n,\n+                    N,\n+                    offs_token,\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n+                    stride_cm,\n+                    stride_cn,\n+                    pid_n,\n+                    N,\n+                    offs_token,\n+                   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n+                    stride_cm,\n+                    stride_cn,\n+                    pid_n,\n+                    N,\n+                    offs_token,\n+                    token+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n+                    stride_cm,\n+                    stride_cn,\n+                    pid_n,\n+                    N,\n+                    offs_token,\n+                    token_mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n+                    stride_cm,\n+                    stride_cn,\n+                    pid_n,\n+                    N,\n+                    offs_token,\n+                    token_mask,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n+                    stride_cm,\n+                    stride_cn,\n+                    pid_n,\n+                    N,\n+                    offs_token,\n+                    token_mask,\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n+                    stride_cm,\n+                    stride_cn,\n+                    pid_n,\n+                    N,\n+                    offs_token,\n+                    token_mask,\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n+                    stride_cm,\n+                    stride_cn,\n+                    pid_n,\n+                    N,\n+                    offs_token,\n+                    token_mask,\n+                   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n+                    stride_cm,\n+                    stride_cn,\n+                    pid_n,\n+                    N,\n+                    offs_token,\n+                    token_mask,\n+                    BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n+                    stride_cm,\n+                    stride_cn,\n+                    pid_n,\n+                    N,\n+                    offs_token,\n+                    token_mask,\n+                    BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n+                    stride_cm,\n+                    stride_cn,\n+                    pid_n,\n+                    N,\n+                    offs_token,\n+                    token_mask,\n+                    BLOCK_SIZE_M+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n+                    stride_cm,\n+                    stride_cn,\n+                    pid_n,\n+                    N,\n+                    offs_token,\n+                    token_mask,\n+                    BLOCK_SIZE_M,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n+                    stride_cm,\n+                    stride_cn,\n+                    pid_n,\n+                    N,\n+                    offs_token,\n+                    token_mask,\n+                    BLOCK_SIZE_M,\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n+                    stride_cm,\n+                    stride_cn,\n+                    pid_n,\n+                    N,\n+                    offs_token,\n+                    token_mask,\n+                    BLOCK_SIZE_M,\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n+                    stride_cm,\n+                    stride_cn,\n+                    pid_n,\n+                    N,\n+                    offs_token,\n+                    token_mask,\n+                    BLOCK_SIZE_M,\n+                   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n+                    stride_cm,\n+                    stride_cn,\n+                    pid_n,\n+                    N,\n+                    offs_token,\n+                    token_mask,\n+                    BLOCK_SIZE_M,\n+                    BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n+                    stride_cm,\n+                    stride_cn,\n+                    pid_n,\n+                    N,\n+                    offs_token,\n+                    token_mask,\n+                    BLOCK_SIZE_M,\n+                    BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n+                    stride_cm,\n+                    stride_cn,\n+                    pid_n,\n+                    N,\n+                    offs_token,\n+                    token_mask,\n+                    BLOCK_SIZE_M,\n+                    BLOCK_SIZE_N+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and MUL_ROUTED_WEIGHT and pid_m * BLOCK_SIZE_M % top_k:\n+            # \u901a\u8fc7\u8bbf\u95eepid_m\u83b7\u53d6\u5df2\u8ba1\u7b97\u5b8c\u6210\u7684\u8f93\u51fa\u503c\uff0c\u4e0e\u5bf9\u5e94\u4f4d\u7f6e\u7684\u6743\u91cd\u76f8\u4e58\n+            # \u4f5c\u4e3a\u6309\u6743\u91cd\u7d2f\u52a0\u7b49\u4ef7\u5b9e\u73b0\n+            accumulator = accumulator * tl.load(topk_weights_ptr + offs_token, mask=token_mask, other=0)\n+            # \u6bcf\u6b21BLOCK_SIZE_K\u4e4b\u540epid_m\u6539\u53d8\u201cchunk\u5c3e\u90e8\u201d\u5047\u4e13\u5bb6id\uff0c\n+            # \u91cd\u65b0\u901a\u8fc7pid_m\u8bbf\u95eeexpert_ids_ptr \u83b7\u53d6\u5bf9\u5e94\u201cchunk\u201d\u65b0\u4f4d\u7f6e\u7684\u4e13\u5bb6id\u3002\n+            # \u5224\u65ad\u662f\u5426\u6709\u6548\uff0c\u65e0\u6548\u5219\u901a\u8fc7\u201creturn\u201d\n+            off_experts = tl.load(expert_ids_ptr + pid_m).to(tl.int64)\n+            if filter_expert and off_experts == -1:\n+                # -----------------------------------------------------------\n+                # Write back zeros to the output when the expert is not\n+                # in the current expert parallel rank.\n+                write_zeros_to_output(\n+                    c_ptr,\n+                    stride_cm,\n+                    stride_cn,\n+                    pid_n,\n+                    N,\n+                    offs_token,\n+                    token_mask,\n+                    BLOCK_SIZE_M,\n+                    BLOCK_SIZE_N,",
 "ttft_seconds": 1.2344418319989927,
 "status": "ok",
 "tg_tokens_per_s": 27.586136085121364,
 "decode_seconds_per_token": 0.036250093050884065,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 35.898642003303394,
  "p95": 45.9236799943028,
  "mean": 36.25009305088407
 },
 "class": "code",
 "requested_input_tokens": 2048
}
