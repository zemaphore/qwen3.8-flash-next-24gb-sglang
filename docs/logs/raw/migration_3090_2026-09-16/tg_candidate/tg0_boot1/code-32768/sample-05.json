{
 "started_utc": "2026-09-16T22:25:19.111492+00:00",
 "wall_seconds": 29.478466832006234,
 "prompt_tokens": 32768,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T22:25:48.590036+00:00",
 "warmup": false,
 "sample_index": 5,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 106,
  "temperature_c": {
   "min": 77.0,
   "max": 79.0,
   "mean": 78.83962264150944
  },
  "power_w": {
   "min": 246.64,
   "max": 281.86,
   "mean": 265.2040566037736
  },
  "util_pct": {
   "min": 39.0,
   "max": 99.0,
   "mean": 92.75471698113208
  },
  "clock_sm_mhz": {
   "min": 1035.0,
   "max": 1905.0,
   "mean": 1729.3867924528302
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 1526.0
  }
 },
 "output_text_sha256": "6c1f4d80edf8b56c9c79b59b8c0c1142b96d332ce1e7bbad208fe9ce5a1063d8",
 "output_text": "++       +        \"\"\"+        \"\"\"Grow+        \"\"\"Grow every+        \"\"\"Grow every layer+        \"\"\"Grow every layer until+        \"\"\"Grow every layer until `+        \"\"\"Grow every layer until `reserve+        \"\"\"Grow every layer until `reserve_mb+        \"\"\"Grow every layer until `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(res+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S ++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Sh+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(res+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(),+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next ==+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autof+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S <+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S ++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free %.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free %.2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free %.2f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free %.2f GB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free %.2f GB\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free %.2f GB\", S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free %.2f GB\", S,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free %.2f GB\", S, torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free %.2f GB\", S, torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free %.2f GB\", S, torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] /+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**3+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    #+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    # ----------------------------------------------------------------+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    # ------------------------------------------------------------------+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    # ------------------------------------------------------------------ control+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    # ------------------------------------------------------------------ control\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    # ------------------------------------------------------------------ control\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    # ------------------------------------------------------------------ control\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    # ------------------------------------------------------------------ control\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    # ------------------------------------------------------------------ control\n+    def poll+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the Mo+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <= int(reserve_mb):\n+                break\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S_next = max(self.s_floor(), S - step)\n+            if S_next == S:\n+                break\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill would keep free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve:\n+                break\n+            S_next = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except RuntimeError:\n+                break\n+            S = S_next\n+        self.pending_regrow = False\n+        logger.info(\"elastic: regrew to S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply",
 "ttft_seconds": 13.283966025002883,
 "status": "ok",
 "tg_tokens_per_s": 31.55410008076161,
 "decode_seconds_per_token": 0.031691602594925386,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 31.456187993171625,
  "p95": 39.83105100633111,
  "mean": 31.691602594925385
 },
 "class": "code",
 "requested_input_tokens": 32768
}
