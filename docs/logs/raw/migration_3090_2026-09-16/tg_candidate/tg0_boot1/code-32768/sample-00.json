{
 "started_utc": "2026-09-16T22:22:42.898939+00:00",
 "wall_seconds": 36.57544506499835,
 "prompt_tokens": 32768,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T22:23:19.474484+00:00",
 "warmup": true,
 "sample_index": 0,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 132,
  "temperature_c": {
   "min": 73.0,
   "max": 78.0,
   "mean": 76.49242424242425
  },
  "power_w": {
   "min": 160.23,
   "max": 279.97,
   "mean": 240.64863636363634
  },
  "util_pct": {
   "min": 0.0,
   "max": 99.0,
   "mean": 74.75
  },
  "clock_sm_mhz": {
   "min": 1200.0,
   "max": 1920.0,
   "mean": 1785.0
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 1390.0
  }
 },
 "output_text_sha256": "4336ac9b0cefb6f2bb0c1245fc48720a08075fe74594cbdf824718c5dabff52b",
 "output_text": "++       +        \"\"\"+        \"\"\"Grow+        \"\"\"Grow every+        \"\"\"Grow every layer+        \"\"\"Grow every layer until+        \"\"\"Grow every layer until `+        \"\"\"Grow every layer until `reserve+        \"\"\"Grow every layer until `reserve_mb+        \"\"\"Grow every layer until `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(res+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) <<+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S <+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S ++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free /+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**3+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Sh+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(res+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) <<+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S >+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free /+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**3+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free //+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total //+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[l+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}\", f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines",
 "ttft_seconds": 20.0814312339935,
 "status": "ok",
 "tg_tokens_per_s": 30.981057188242893,
 "decode_seconds_per_token": 0.03227778813111302,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 32.09562100528274,
  "p95": 41.58914698928129,
  "mean": 32.27778813111302
 },
 "class": "code",
 "requested_input_tokens": 32768
}
