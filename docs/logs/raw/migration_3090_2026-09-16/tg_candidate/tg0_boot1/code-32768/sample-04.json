{
 "started_utc": "2026-09-16T22:24:49.155935+00:00",
 "wall_seconds": 29.952369833001285,
 "prompt_tokens": 32768,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T22:25:19.108383+00:00",
 "warmup": false,
 "sample_index": 4,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 107,
  "temperature_c": {
   "min": 77.0,
   "max": 79.0,
   "mean": 78.5607476635514
  },
  "power_w": {
   "min": 241.96,
   "max": 281.68,
   "mean": 263.32112149532713
  },
  "util_pct": {
   "min": 88.0,
   "max": 99.0,
   "mean": 94.77570093457943
  },
  "clock_sm_mhz": {
   "min": 1185.0,
   "max": 1920.0,
   "mean": 1733.411214953271
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 1528.0
  }
 },
 "output_text_sha256": "ba2c347737a78d050379ea5e69a5c88688f43e3c7fdb237ea1ca6e6fb5b1692d",
 "output_text": "++       +        \"\"\"+        \"\"\"Grow+        \"\"\"Grow every+        \"\"\"Grow every layer+        \"\"\"Grow every layer until+        \"\"\"Grow every layer until `+        \"\"\"Grow every layer until `reserve+        \"\"\"Grow every layer until `reserve_mb+        \"\"\"Grow every layer until `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(res+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S ++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(),+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Sh+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(res+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(),+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S ==+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                              +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(res+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb),+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(),+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    #+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ----------------------------------------------------------------+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the Mo+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (pref+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/e+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls +=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls %+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 1+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 10+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 ==+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._aut+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autof+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 <+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._aut+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autof+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._aut+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autof+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >> 20 >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >> 20 >= self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >> 20 >= self.reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >> 20 >= self.regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >> 20 >= self.regrow_reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >> 20 >= self.regrow_reserve:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >> 20 >= self.regrow_reserve:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >> 20 >= self.regrow_reserve:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >> 20 >= self.regrow_reserve:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >> 20 >= self.regrow_reserve:\n+            self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >> 20 >= self.regrow_reserve:\n+            self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >> 20 >= self.regrow_reserve:\n+            self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >> 20 >= self.regrow_reserve:\n+            self.set_S(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >> 20 >= self.regrow_reserve:\n+            self.set_S(self.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >> 20 >= self.regrow_reserve:\n+            self.set_S(self.S0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >> 20 >= self.regrow_reserve:\n+            self.set_S(self.S0)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >> 20 >= self.regrow_reserve:\n+            self.set_S(self.S0)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >> 20 >= self.regrow_reserve:\n+            self.set_S(self.S0)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >> 20 >= self.regrow_reserve:\n+            self.set_S(self.S0)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >> 20 >= self.regrow_reserve:\n+            self.set_S(self.S0)\n+            self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >> 20 >= self.regrow_reserve:\n+            self.set_S(self.S0)\n+            self.pending+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >> 20 >= self.regrow_reserve:\n+            self.set_S(self.S0)\n+            self.pending_reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >> 20 >= self.regrow_reserve:\n+            self.set_S(self.S0)\n+            self.pending_regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >> 20 >= self.regrow_reserve:\n+            self.set_S(self.S0)\n+            self.pending_regrow =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >> 20 >= self.regrow_reserve:\n+            self.set_S(self.S0)\n+            self.pending_regrow = False+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >> 20 >= self.regrow_reserve:\n+            self.set_S(self.S0)\n+            self.pending_regrow = False\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >> 20 >= self.regrow_reserve:\n+            self.set_S(self.S0)\n+            self.pending_regrow = False\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >> 20 >= self.regrow_reserve:\n+            self.set_S(self.S0)\n+            self.pending_regrow = False\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >> 20 >= self.regrow_reserve:\n+            self.set_S(self.S0)\n+            self.pending_regrow = False\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >> 20 >= self.regrow_reserve:\n+            self.set_S(self.S0)\n+            self.pending_regrow = False\n+        if not+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >> 20 >= self.regrow_reserve:\n+            self.set_S(self.S0)\n+            self.pending_regrow = False\n+        if not self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >> 20 >= self.regrow_reserve:\n+            self.set_S(self.S0)\n+            self.pending_regrow = False\n+        if not self.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >> 20 >= self.regrow_reserve:\n+            self.set_S(self.S0)\n+            self.pending_regrow = False\n+        if not self.ctl+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >> 20 >= self.regrow_reserve:\n+            self.set_S(self.S0)\n+            self.pending_regrow = False\n+        if not self.ctl:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >> 20 >= self.regrow_reserve:\n+            self.set_S(self.S0)\n+            self.pending_regrow = False\n+        if not self.ctl:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >> 20 >= self.regrow_reserve:\n+            self.set_S(self.S0)\n+            self.pending_regrow = False\n+        if not self.ctl:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception:\n+                for S_ in range(S - step, self.s_floor(), -step):\n+                    try:\n+                        self.set_S(S_)\n+                        break\n+                    except Exception:\n+                        pass\n+                else:\n+                    raise\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            if S == self.s_floor():\n+                logger.warning(\"elastic: cannot free %d MB (S_floor=%d, VRAM free %d MB)\",\n+                               int(reserve_mb), self.s_floor(), free)\n+                break\n+            self.set_S(S)\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None and torch.cuda.mem_get_info()[0] >> 20 < self._autofill:\n+            self.fill(self._autofill)\n+        elif self.pending_regrow and torch.cuda.mem_get_info()[0] >> 20 >= self.regrow_reserve:\n+            self.set_S(self.S0)\n+            self.pending_regrow = False\n+        if not self.ctl:\n+           ",
 "ttft_seconds": 13.279408490008791,
 "status": "ok",
 "tg_tokens_per_s": 30.648526463619994,
 "decode_seconds_per_token": 0.03262799603716696,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 32.25559000566136,
  "p95": 41.820283004199155,
  "mean": 32.627996037166966
 },
 "class": "code",
 "requested_input_tokens": 32768
}
