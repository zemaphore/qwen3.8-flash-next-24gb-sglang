{
 "started_utc": "2026-09-16T22:23:19.477390+00:00",
 "wall_seconds": 30.016214226998272,
 "prompt_tokens": 32768,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T22:23:49.493682+00:00",
 "warmup": false,
 "sample_index": 1,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 108,
  "temperature_c": {
   "min": 77.0,
   "max": 78.0,
   "mean": 77.8425925925926
  },
  "power_w": {
   "min": 238.32,
   "max": 279.75,
   "mean": 262.94787037037037
  },
  "util_pct": {
   "min": 67.0,
   "max": 99.0,
   "mean": 93.46296296296296
  },
  "clock_sm_mhz": {
   "min": 1305.0,
   "max": 1920.0,
   "mean": 1740.2777777777778
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 1526.0
  }
 },
 "output_text_sha256": "d9a88d36de43c5d53146eed4b686e2bffd2251d1ca4d5b566231069bad3dea7f",
 "output_text": "++       +        \"\"\"+        \"\"\"Grow+        \"\"\"Grow every+        \"\"\"Grow every layer+        \"\"\"Grow every layer until+        \"\"\"Grow every layer until `+        \"\"\"Grow every layer until `reserve+        \"\"\"Grow every layer until `reserve_mb+        \"\"\"Grow every layer until `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(res+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S ++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Sh+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(res+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[l+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w1+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_q+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl ++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\")+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) ++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def poll+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def poll(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def poll(self):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def poll(self):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def poll(self):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def poll(self):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def poll(self):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def poll(self):\n+        \"\"\"Called+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def poll(self):\n+        \"\"\"Called from+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def poll(self):\n+        \"\"\"Called from the+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def poll(self):\n+        \"\"\"Called from the Mo+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (pref+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/e+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls +=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls %+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.s_floor()\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S >= self.E:\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb) or S <= self.s_floor():\n+                break\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S_floor={self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena >> 20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 1",
 "ttft_seconds": 13.23930498601112,
 "status": "ok",
 "tg_tokens_per_s": 30.458659986748234,
 "decode_seconds_per_token": 0.03283138524265591,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 32.339323006453924,
  "p95": 41.850206995150074,
  "mean": 32.831385242655905
 },
 "class": "code",
 "requested_input_tokens": 32768
}
