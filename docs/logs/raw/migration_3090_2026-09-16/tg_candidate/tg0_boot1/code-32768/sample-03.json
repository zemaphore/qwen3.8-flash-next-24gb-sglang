{
 "started_utc": "2026-09-16T22:24:19.367183+00:00",
 "wall_seconds": 29.78353529999731,
 "prompt_tokens": 32768,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T22:24:49.150868+00:00",
 "warmup": false,
 "sample_index": 3,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 107,
  "temperature_c": {
   "min": 77.0,
   "max": 79.0,
   "mean": 78.19626168224299
  },
  "power_w": {
   "min": 244.09,
   "max": 281.49,
   "mean": 264.4271028037383
  },
  "util_pct": {
   "min": 40.0,
   "max": 99.0,
   "mean": 92.50467289719626
  },
  "clock_sm_mhz": {
   "min": 1200.0,
   "max": 1920.0,
   "mean": 1739.8598130841121
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 1448.0
  }
 },
 "output_text_sha256": "e6b33b60bd9da734b7e52ed5bc5ef94b43728a9e70225740cad874251530c15b",
 "output_text": "++       +        \"\"\"+        \"\"\"Grow+        \"\"\"Grow every+        \"\"\"Grow every layer+        \"\"\"Grow every layer until+        \"\"\"Grow every layer until `+        \"\"\"Grow every layer until `reserve+        \"\"\"Grow every layer until `reserve_mb+        \"\"\"Grow every layer until `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(res+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) <<+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S <+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S ++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(l+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except Arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step ==+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Sh+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(res+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) <<+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S >+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    #+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ----------------------------------------------------------------+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the Mo+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (pref+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/e+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls +=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls %+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 1+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 10+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 ==+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._aut+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autof+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._aut+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autof+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill:\n+                self.fill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill:\n+                self.fill(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill:\n+                self.fill(self._+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill:\n+                self.fill(self._aut+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill:\n+                self.fill(self._autof+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill:\n+                self.fill(self._autofill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill:\n+                self.fill(self._autofill)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill:\n+                self.fill(self._autofill)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill:\n+                self.fill(self._autofill)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill:\n+                self.fill(self._autofill)\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill:\n+                self.fill(self._autofill)\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill:\n+                self.fill(self._autofill)\n+                self.pending+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill:\n+                self.fill(self._autofill)\n+                self.pending_reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill:\n+                self.fill(self._autofill)\n+                self.pending_regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill:\n+                self.fill(self._autofill)\n+                self.pending_regrow =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill:\n+                self.fill(self._autofill)\n+                self.pending_regrow = True+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill:\n+                self.fill(self._autofill)\n+                self.pending_regrow = True\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill:\n+                self.fill(self._autofill)\n+                self.pending_regrow = True\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill:\n+                self.fill(self._autofill)\n+                self.pending_regrow = True\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill:\n+                self.fill(self._autofill)\n+                self.pending_regrow = True\n+        elif+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill:\n+                self.fill(self._autofill)\n+                self.pending_regrow = True\n+        elif self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill:\n+                self.fill(self._autofill)\n+                self.pending_regrow = True\n+        elif self.pending+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill:\n+                self.fill(self._autofill)\n+                self.pending_regrow = True\n+        elif self.pending_reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill:\n+                self.fill(self._autofill)\n+                self.pending_regrow = True\n+        elif self.pending_regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill:\n+                self.fill(self._autofill)\n+                self.pending_regrow = True\n+        elif self.pending_regrow:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill:\n+                self.fill(self._autofill)\n+                self.pending_regrow = True\n+        elif self.pending_regrow:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill:\n+                self.fill(self._autofill)\n+                self.pending_regrow = True\n+        elif self.pending_regrow:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill:\n+                self.fill(self._autofill)\n+                self.pending_regrow = True\n+        elif self.pending_regrow:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill:\n+                self.fill(self._autofill)\n+                self.pending_regrow = True\n+        elif self.pending_regrow:\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill:\n+                self.fill(self._autofill)\n+                self.pending_regrow = True\n+        elif self.pending_regrow:\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill:\n+                self.fill(self._autofill)\n+                self.pending_regrow = True\n+        elif self.pending_regrow:\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill:\n+                self.fill(self._autofill)\n+                self.pending_regrow = True\n+        elif self.pending_regrow:\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill:\n+                self.fill(self._autofill)\n+                self.pending_regrow = True\n+        elif self.pending_regrow:\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill:\n+                self.fill(self._autofill)\n+                self.pending_regrow = True\n+        elif self.pending_regrow:\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill:\n+                self.fill(self._autofill)\n+                self.pending_regrow = True\n+        elif self.pending_regrow:\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill:\n+                self.fill(self._autofill)\n+                self.pending_regrow = True\n+        elif self.pending_regrow:\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill:\n+                self.fill(self._autofill)\n+                self.pending_regrow = True\n+        elif self.pending_regrow:\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill:\n+                self.fill(self._autofill)\n+                self.pending_regrow = True\n+        elif self.pending_regrow:\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill:\n+                self.fill(self._autofill)\n+                self.pending_regrow = True\n+        elif self.pending_regrow:\n+            free = torch.cuda.mem_get_info()[0] >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill:\n+                self.fill(self._autofill)\n+                self.pending_regrow = True\n+        elif self.pending_regrow:\n+            free = torch.cuda.mem_get_info()[0] >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill:\n+                self.fill(self._autofill)\n+                self.pending_regrow = True\n+        elif self.pending_regrow:\n+            free = torch.cuda.mem_get_info()[0] >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill:\n+                self.fill(self._autofill)\n+                self.pending_regrow = True\n+        elif self.pending_regrow:\n+            free = torch.cuda.mem_get_info()[0] >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                return S\n+            S_next = min(S + step, self.E)\n+            try:\n+                for lid in sorted(self.layers):\n+                    self.resize_layer(lid, S_next)\n+                S = S_next\n+            except ArenaOOM:\n+                torch.cuda.empty_cache()\n+                if torch.cuda.mem_get_info()[0] <= reserve:\n+                    return S\n+                step //= 2\n+                if step == 0:\n+                    logger.info(\"elastic fill: VRAM exhausted at S=%d\", S)\n+                    return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                return S\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            S = S_next\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self._autofill:\n+                self.fill(self._autofill)\n+                self.pending_regrow = True\n+        elif self.pending_regrow:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n",
 "ttft_seconds": 13.210010291993967,
 "status": "ok",
 "tg_tokens_per_s": 30.832430674402833,
 "decode_seconds_per_token": 0.03243338193346536,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 32.447336008772254,
  "p95": 40.62126200005878,
  "mean": 32.433381933465355
 },
 "class": "code",
 "requested_input_tokens": 32768
}
