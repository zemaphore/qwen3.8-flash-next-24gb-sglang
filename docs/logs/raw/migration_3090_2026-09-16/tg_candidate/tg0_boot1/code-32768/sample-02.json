{
 "started_utc": "2026-09-16T22:23:49.498646+00:00",
 "wall_seconds": 29.863318240997614,
 "prompt_tokens": 32768,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T22:24:19.362162+00:00",
 "warmup": false,
 "sample_index": 2,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 107,
  "temperature_c": {
   "min": 76.0,
   "max": 79.0,
   "mean": 78.02803738317758
  },
  "power_w": {
   "min": 244.09,
   "max": 279.99,
   "mean": 264.54841121495326
  },
  "util_pct": {
   "min": 40.0,
   "max": 99.0,
   "mean": 93.16822429906541
  },
  "clock_sm_mhz": {
   "min": 1395.0,
   "max": 1920.0,
   "mean": 1750.3738317757009
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 1448.0
  }
 },
 "output_text_sha256": "8bf651fdedebbb19b546e15489e9e8213eb5b35fd39f0fdbf8466543836ee9d2",
 "output_text": "++       +        \"\"\"+        \"\"\"Grow+        \"\"\"Grow every+        \"\"\"Grow every layer+        \"\"\"Grow every layer until+        \"\"\"Grow every layer until `+        \"\"\"Grow every layer until `reserve+        \"\"\"Grow every layer until `reserve_mb+        \"\"\"Grow every layer until `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] /+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S ++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Sh+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] /+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free //+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total //+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[l+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena //+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w1+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_q+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl ++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\")+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) ++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def poll+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def poll(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def poll(self):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def poll(self):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the arena at its old size).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb or S <= 0:\n+                break\n+            S_new = max(S - step, 0)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"total {total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.nbytes for k in st.kinds.values())\n+            lines.append(f\"L{lid} S={st.S} arena={arena // 2**20}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def poll(self):\n+",
 "ttft_seconds": 13.289667336997809,
 "status": "ok",
 "tg_tokens_per_s": 30.83221900524999,
 "decode_seconds_per_token": 0.0324336045949117,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 32.15251199435443,
  "p95": 41.471895994618535,
  "mean": 32.4336045949117
 },
 "class": "code",
 "requested_input_tokens": 32768
}
