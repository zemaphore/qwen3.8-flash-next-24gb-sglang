{
 "started_utc": "2026-09-16T22:18:59.550878+00:00",
 "wall_seconds": 14.649390634993324,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T22:19:14.200378+00:00",
 "warmup": false,
 "sample_index": 3,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 53,
  "temperature_c": {
   "min": 74.0,
   "max": 75.0,
   "mean": 74.30188679245283
  },
  "power_w": {
   "min": 259.07,
   "max": 277.09,
   "mean": 266.2147169811321
  },
  "util_pct": {
   "min": 89.0,
   "max": 100.0,
   "mean": 91.50943396226415
  },
  "clock_sm_mhz": {
   "min": 1620.0,
   "max": 1920.0,
   "mean": 1891.4150943396226
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2540.0
  }
 },
 "output_text_sha256": "0bacad909ba7f9da3835880c7e6992dbf9ce4c744c5414ec4c7ceb3c46dc2d69",
 "output_text": ".py.py --.py --url.py --url http.py --url http://.py --url http://1.py --url http://12.py --url http://127.py --url http://127..py --url http://127.0.py --url http://127.0..py --url http://127.0.0.py --url http://127.0.0..py --url http://127.0.0.1.py --url http://127.0.0.1:.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30.py --url http://127.0.0.1:300.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:300.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches S.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`;.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 26.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,14.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:44.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448)..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`);.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`)..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:44.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448)..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsg.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (q.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-7.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nk.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 Mo.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gem.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gem.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher),.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the S.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command),.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log),.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/      .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state),.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n###.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sg.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/q.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-7.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, generated.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, generated from.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, generated from the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, generated from the layered.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, generated from the layered patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, generated from the layered patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, generated from the layered patch scripts\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, generated from the layered patch scripts\nin.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, generated from the layered patch scripts\nin `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, generated from the layered patch scripts\nin `patches.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, generated from the layered patch scripts\nin `patches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, generated from the layered patch scripts\nin `patches/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, generated from the layered patch scripts\nin `patches/` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, generated from the layered patch scripts\nin `patches/` (CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, generated from the layered patch scripts\nin `patches/` (CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, generated from the layered patch scripts\nin `patches/` (CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, generated from the layered patch scripts\nin `patches/` (CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, generated from the layered patch scripts\nin `patches/` (CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, generated from the layered patch scripts\nin `patches/` (CAMPAIGN.md:1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, generated from the layered patch scripts\nin `patches/` (CAMPAIGN.md:15.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, generated from the layered patch scripts\nin `patches/` (CAMPAIGN.md:150.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, generated from the layered patch scripts\nin `patches/` (CAMPAIGN.md:150)..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, generated from the layered patch scripts\nin `patches/` (CAMPAIGN.md:150). It.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, generated from the layered patch scripts\nin `patches/` (CAMPAIGN.md:150). It is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, generated from the layered patch scripts\nin `patches/` (CAMPAIGN.md:150). It is applied.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, generated from the layered patch scripts\nin `patches/` (CAMPAIGN.md:150). It is applied to.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `ple/`); the checkpoint from `MODEL` (default `model/`).\n`--enable-ple` requires `ple/ple.json` and `ple/ple.f8_e4m3.bin` under `PLE` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the PLE patches\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather, PLE n-gram hash\nscripts/        serve.sh (the launcher), sg.sh (the SGLang command), the build and test scripts\ntools/        bench.py, ple.py, the KV-tier and elastic-control tools, the PLE tools, the quality tools\ndocs/         CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       phase1_state.json (the accepted state), the PLE table, the PLE model config\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, generated from the layered patch scripts\nin `patches/` (CAMPAIGN.md:150). It is applied to S",
 "ttft_seconds": 1.1030381729942746,
 "status": "ok",
 "tg_tokens_per_s": 37.7225473534927,
 "decode_seconds_per_token": 0.026509344414870507,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 25.98433700040914,
  "p95": 33.40402300818823,
  "mean": 26.509344414870508
 },
 "class": "prose",
 "requested_input_tokens": 2048
}
