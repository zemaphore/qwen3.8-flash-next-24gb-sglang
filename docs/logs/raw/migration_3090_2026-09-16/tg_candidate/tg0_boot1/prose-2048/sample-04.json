{
 "started_utc": "2026-09-16T22:19:14.205689+00:00",
 "wall_seconds": 14.739422414990258,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T22:19:28.945309+00:00",
 "warmup": false,
 "sample_index": 4,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 53,
  "temperature_c": {
   "min": 74.0,
   "max": 76.0,
   "mean": 75.01886792452831
  },
  "power_w": {
   "min": 258.68,
   "max": 276.06,
   "mean": 266.7177358490566
  },
  "util_pct": {
   "min": 88.0,
   "max": 100.0,
   "mean": 91.18867924528301
  },
  "clock_sm_mhz": {
   "min": 1620.0,
   "max": 1920.0,
   "mean": 1899.622641509434
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2540.0
  }
 },
 "output_text_sha256": "616c509daa0307cbc1259f5233acd28a9df015f435b5759f3d88ea508fbd9c0a",
 "output_text": ".py.py --.py --url.py --url http.py --url http://.py --url http://1.py --url http://12.py --url http://127.py --url http://127..py --url http://127.0.py --url http://127.0..py --url http://127.0.0.py --url http://127.0.0..py --url http://127.0.0.1.py --url http://127.0.0.1:.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30.py --url http://127.0.0.1:300.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 .py --url http://127.0.0.1:30000 --prompt-tokens 101 4.py --url http://127.0.0.1:30000 --prompt-tokens 101 42.py --url http://127.0.0.1:30000 --prompt-tokens 101 421.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 17.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 170.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 68.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 682.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 100.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1000.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nS.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 26.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 2621.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 26214.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-g.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsg.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 7.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 Mo.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gem.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gem.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/         .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n               .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/          .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n               .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository\n\n###.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository\n\n### The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository\n\n### The serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository\n\n### The serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository\n\n### The serving patch\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository\n\n### The serving patch\n\n[`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository\n\n### The serving patch\n\n[`sg.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository\n\n### The serving patch\n\n[`sglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository\n\n### The serving patch\n\n[`sglang/q.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository\n\n### The serving patch\n\n[`sglang/qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository\n\n### The serving patch\n\n[`sglang/qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository\n\n### The serving patch\n\n[`sglang/qwen4exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository\n\n### The serving patch\n\n[`sglang/qwen4exp-serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository\n\n### The serving patch\n\n[`sglang/qwen4exp-serving-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository\n\n### The serving patch\n\n[`sglang/qwen4exp-serving-7.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository\n\n### The serving patch\n\n[`sglang/qwen4exp-serving-73.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository\n\n### The serving patch\n\n[`sglang/qwen4exp-serving-73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository\n\n### The serving patch\n\n[`sglang/qwen4exp-serving-73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository\n\n### The serving patch\n\n[`sglang/qwen4exp-serving-73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository\n\n### The serving patch\n\n[`sglang/qwen4exp-serving-73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository\n\n### The serving patch\n\n[`sglang/qwen4exp-serving-73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository\n\n### The serving patch\n\n[`sglang/qwen4exp-serving-73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository\n\n### The serving patch\n\n[`sglang/qwen4exp-serving-73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository\n\n### The serving patch\n\n[`sglang/qwen4exp-serving-73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository\n\n### The serving patch\n\n[`sglang/qwen4exp-serving-73a255206f.patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository\n\n### The serving patch\n\n[`sglang/qwen4exp-serving-73a255206f.patch`](.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository\n\n### The serving patch\n\n[`sglang/qwen4exp-serving-73a255206f.patch`](sg.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository\n\n### The serving patch\n\n[`sglang/qwen4exp-serving-73a255206f.patch`](sglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository\n\n### The serving patch\n\n[`sglang/qwen4exp-serving-73a255206f.patch`](sglang/q.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository\n\n### The serving patch\n\n[`sglang/qwen4exp-serving-73a255206f.patch`](sglang/qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository\n\n### The serving patch\n\n[`sglang/qwen4exp-serving-73a255206f.patch`](sglang/qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository\n\n### The serving patch\n\n[`sglang/qwen4exp-serving-73a255206f.patch`](sglang/qwen4exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository\n\n### The serving patch\n\n[`sglang/qwen4exp-serving-73a255206f.patch`](sglang/qwen4exp-serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository\n\n### The serving patch\n\n[`sglang/qwen4exp-serving-73a255206f.patch`](sglang/qwen4exp-serving-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository\n\n### The serving patch\n\n[`sglang/qwen4exp-serving-73a255206f.patch`](sglang/qwen4exp-serving-7.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository\n\n### The serving patch\n\n[`sglang/qwen4exp-serving-73a255206f.patch`](sglang/qwen4exp-serving-73.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository\n\n### The serving patch\n\n[`sglang/qwen4exp-serving-73a255206f.patch`](sglang/qwen4exp-serving-73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository\n\n### The serving patch\n\n[`sglang/qwen4exp-serving-73a255206f.patch`](sglang/qwen4exp-serving-73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository\n\n### The serving patch\n\n[`sglang/qwen4exp-serving-73a255206f.patch`](sglang/qwen4exp-serving-73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository\n\n### The serving patch\n\n[`sglang/qwen4exp-serving-73a255206f.patch`](sglang/qwen4exp-serving-73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository\n\n### The serving patch\n\n[`sglang/qwen4exp-serving-73a255206f.patch`](sglang/qwen4exp-serving-73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository\n\n### The serving patch\n\n[`sglang/qwen4exp-serving-73a255206f.patch`](sglang/qwen4exp-serving-73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository\n\n### The serving patch\n\n[`sglang/qwen4exp-serving-73a255206f.patch`](sglang/qwen4exp-serving-73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository\n\n### The serving patch\n\n[`sglang/qwen4exp-serving-73a255206f.patch`](sglang/qwen4exp-serving-73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository\n\n### The serving patch\n\n[`sglang/qwen4exp-serving-73a255206f.patch`](sglang/qwen4exp-serving-73a255206f.patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository\n\n### The serving patch\n\n[`sglang/qwen4exp-serving-73a255206f.patch`](sglang/qwen4exp-serving-73a255206f.patch).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.f8_e4m3.bin` and `--ple-table-max-gib 1.0`. The `PLE` variable\nmust point at the table file itself, not at the `ple/` directory. The server's own log records the\nconfiguration it started with, including the `ple` line and the `kv_tiers` line.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git diff against commit 73a255206f) and its README\npatches/        the layered patch scripts the serving patch was built from, plus the serving environment\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE, elastic expert cache\nassets/         the two serving assets: the PLE n-gram table and the expert-cache floor checkpoint\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.log\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nscripts/        serve.sh (the launcher of record), build_ple.sh, build_ple_test.sh,\n                build_ple_validate.sh, build_ple_validate_test.sh, build_ple_validate_test.log\n```\n\n## What is in the repository\n\n### The serving patch\n\n[`sglang/qwen4exp-serving-73a255206f.patch`](sglang/qwen4exp-serving-73a255206f.patch) is",
 "ttft_seconds": 1.1136447889875853,
 "status": "ok",
 "tg_tokens_per_s": 37.50270899000646,
 "decode_seconds_per_token": 0.026664740412925242,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 26.581938000163063,
  "p95": 33.0604609916918,
  "mean": 26.66474041292524
 },
 "class": "prose",
 "requested_input_tokens": 2048
}
