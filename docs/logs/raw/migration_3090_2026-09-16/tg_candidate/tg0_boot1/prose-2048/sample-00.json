{
 "started_utc": "2026-09-16T22:18:13.835643+00:00",
 "wall_seconds": 16.277153276998433,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T22:18:30.112901+00:00",
 "warmup": true,
 "sample_index": 0,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 59,
  "temperature_c": {
   "min": 62.0,
   "max": 71.0,
   "mean": 69.11864406779661
  },
  "power_w": {
   "min": 115.35,
   "max": 264.44,
   "mean": 242.22152542372882
  },
  "util_pct": {
   "min": 0.0,
   "max": 95.0,
   "mean": 77.98305084745763
  },
  "clock_sm_mhz": {
   "min": 1725.0,
   "max": 1935.0,
   "mean": 1909.5762711864406
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2540.0
  }
 },
 "output_text_sha256": "68aa09b8da50490a55439bc391495d65e8cf1dfe931fe76334c28841804f0a18",
 "output_text": ".py.py --.py --url.py --url http.py --url http://.py --url http://1.py --url http://12.py --url http://127.py --url http://127..py --url http://127.0.py --url http://127.0..py --url http://127.0.0.py --url http://127.0.0..py --url http://127.0.0.1.py --url http://127.0.0.1:.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30.py --url http://127.0.0.1:300.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 .py --url http://127.0.0.1:30000 --prompt-tokens 101 4.py --url http://127.0.0.1:30000 --prompt-tokens 101 42.py --url http://127.0.0.1:30000 --prompt-tokens 101 421.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 17.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 170.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 68.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 682.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 100.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1000.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers;.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nS.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 26.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 2621.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 26214.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-g.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n12.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quant.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-ev.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-d.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache ev.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-d.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-d.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-d.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-read.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-d.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-w.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-d.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nread.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-d.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-read.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nread.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-d.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-read.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-d.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-read.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-d.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-read.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--ple-d.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--ple-disk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--ple-disk-read.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--ple-disk-readers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--ple-disk-readers-active.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--ple-disk-readers-active-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--ple-disk-readers-active-async.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--ple-disk-readers-active-async-p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--ple-disk-readers-active-async-pool.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--ple-disk-readers-active-async-pool .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--ple-disk-readers-active-async-pool 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--ple-disk-readers-active-async-pool 2`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--ple-disk-readers-active-async-pool 2` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--ple-disk-readers-active-async-pool 2` (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--ple-disk-readers-active-async-pool 2` (the async.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--ple-disk-readers-active-async-pool 2` (the async P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--ple-disk-readers-active-async-pool 2` (the async PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--ple-disk-readers-active-async-pool 2` (the async PLE disk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--ple-disk-readers-active-async-pool 2` (the async PLE disk reader.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--ple-disk-readers-active-async-pool 2` (the async PLE disk reader pool.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--ple-disk-readers-active-async-pool 2` (the async PLE disk reader pool at.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--ple-disk-readers-active-async-pool 2` (the async PLE disk reader pool at .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--ple-disk-readers-active-async-pool 2` (the async PLE disk reader pool at 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--ple-disk-readers-active-async-pool 2` (the async PLE disk reader pool at 2),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--ple-disk-readers-active-async-pool 2` (the async PLE disk reader pool at 2), `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--ple-disk-readers-active-async-pool 2` (the async PLE disk reader pool at 2), `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--ple-disk-readers-active-async-pool 2` (the async PLE disk reader pool at 2), `--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--ple-disk-readers-active-async-pool 2` (the async PLE disk reader pool at 2), `--ple-d.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--ple-disk-readers-active-async-pool 2` (the async PLE disk reader pool at 2), `--ple-disk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--ple-disk-readers-active-async-pool 2` (the async PLE disk reader pool at 2), `--ple-disk-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--ple-disk-readers-active-async-pool 2` (the async PLE disk reader pool at 2), `--ple-disk-\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--ple-disk-readers-active-async-pool 2` (the async PLE disk reader pool at 2), `--ple-disk-\nread.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--ple-disk-readers-active-async-pool 2` (the async PLE disk reader pool at 2), `--ple-disk-\nreaders.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--ple-disk-readers-active-async-pool 2` (the async PLE disk reader pool at 2), `--ple-disk-\nreaders-active.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--ple-disk-readers-active-async-pool 2` (the async PLE disk reader pool at 2), `--ple-disk-\nreaders-active-sync.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--ple-disk-readers-active-async-pool 2` (the async PLE disk reader pool at 2), `--ple-disk-\nreaders-active-sync-p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--ple-disk-readers-active-async-pool 2` (the async PLE disk reader pool at 2), `--ple-disk-\nreaders-active-sync-pool.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--ple-disk-readers-active-async-pool 2` (the async PLE disk reader pool at 2), `--ple-disk-\nreaders-active-sync-pool .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--ple-disk-readers-active-async-pool 2` (the async PLE disk reader pool at 2), `--ple-disk-\nreaders-active-sync-pool 0.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--ple-disk-readers-active-async-pool 2` (the async PLE disk reader pool at 2), `--ple-disk-\nreaders-active-sync-pool 0`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--ple-disk-readers-active-async-pool 2` (the async PLE disk reader pool at 2), `--ple-disk-\nreaders-active-sync-pool 0` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--ple-disk-readers-active-async-pool 2` (the async PLE disk reader pool at 2), `--ple-disk-\nreaders-active-sync-pool 0` (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--ple-disk-readers-active-async-pool 2` (the async PLE disk reader pool at 2), `--ple-disk-\nreaders-active-sync-pool 0` (the sync.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--ple-disk-readers-active-async-pool 2` (the async PLE disk reader pool at 2), `--ple-disk-\nreaders-active-sync-pool 0` (the sync P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--ple-disk-readers-active-async-pool 2` (the async PLE disk reader pool at 2), `--ple-disk-\nreaders-active-sync-pool 0` (the sync PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--ple-disk-readers-active-async-pool 2` (the async PLE disk reader pool at 2), `--ple-disk-\nreaders-active-sync-pool 0` (the sync PLE disk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple\n--ple-table-path` (the PLE n-gram table), `--ple-gpu-cache-size 128` (the PLE GPU cache), `--ple-ram-cache-size\n128` (the PLE RAM cache), `--ple-ram-cache-mode int8` (the PLE RAM cache quantized to INT8), `--ple-ram-cache-\nmax-size 16` (the PLE RAM cache capped at 16 GB), `--ple-ram-cache-evict-to-disk` (the PLE RAM cache evicts\nto disk), `--ple-disk-path` (the PLE disk path), `--ple-disk-size 16` (the PLE disk cache capped at 16 GB),\n`--ple-disk-readers 4` (four PLE disk readers), `--ple-disk-writer 1` (one PLE disk writer), `--ple-disk-\nreaders-active 2` (two active PLE disk readers), `--ple-disk-readers-active-async 2` (two async PLE disk\nreaders), `--ple-disk-readers-active-sync 0` (no sync PLE disk readers), `--ple-disk-readers-active-async-\nmax 2` (the async PLE disk reader cap at 2), `--ple-disk-readers-active-sync-max 0` (the sync PLE disk reader\ncap at 0), `--ple-disk-readers-active-async-pool 2` (the async PLE disk reader pool at 2), `--ple-disk-\nreaders-active-sync-pool 0` (the sync PLE disk reader",
 "ttft_seconds": 2.8844969979982125,
 "status": "ok",
 "tg_tokens_per_s": 38.15564125658719,
 "decode_seconds_per_token": 0.02620844433658575,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 25.814634005655535,
  "p95": 32.74909300671425,
  "mean": 26.20844433658575
 },
 "class": "prose",
 "requested_input_tokens": 2048
}
