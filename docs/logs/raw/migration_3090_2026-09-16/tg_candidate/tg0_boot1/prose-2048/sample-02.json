{
 "started_utc": "2026-09-16T22:18:44.951265+00:00",
 "wall_seconds": 14.597744448008598,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T22:18:59.549110+00:00",
 "warmup": false,
 "sample_index": 2,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 52,
  "temperature_c": {
   "min": 72.0,
   "max": 74.0,
   "mean": 73.26923076923077
  },
  "power_w": {
   "min": 246.12,
   "max": 275.34,
   "mean": 262.7838461538462
  },
  "util_pct": {
   "min": 89.0,
   "max": 100.0,
   "mean": 91.07692307692308
  },
  "clock_sm_mhz": {
   "min": 1665.0,
   "max": 1920.0,
   "mean": 1898.6538461538462
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2540.0
  }
 },
 "output_text_sha256": "5ac6125d2a88c27fa1318dc586b7b6deaddd02e7139f6f574403bdd5fca52e5b",
 "output_text": ".py.py --.py --url.py --url http.py --url http://.py --url http://1.py --url http://12.py --url http://127.py --url http://127..py --url http://127.0.py --url http://127.0..py --url http://127.0.0.py --url http://127.0.0..py --url http://127.0.0.1.py --url http://127.0.0.1:.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30.py --url http://127.0.0.1:300.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 .py --url http://127.0.0.1:30000 --prompt-tokens 101 4.py --url http://127.0.0.1:30000 --prompt-tokens 101 42.py --url http://127.0.0.1:30000 --prompt-tokens 101 421.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 17.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 170.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 68.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 682.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 100.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1000.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches S.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`;.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 26.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,14.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:44.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448)..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` +.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POP.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` +.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `M.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_W.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:44.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448)..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:44.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448)..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:44.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448)..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsg.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (q.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-7.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 Mo.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gem.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_b.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_b.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_b.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n           .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_b.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_b.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_b.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_b.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_b.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,\n           .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,\n            n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,\n            ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,\n            ngram_b.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,\n            ngram_bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,\n            ngram_bench9.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,\n            ngram_bench9.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,\n            ngram_bench9.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,\n            ngram_bench9.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,\n            ngram_bench9.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,\n            ngram_bench9.py, ngram_b.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,\n            ngram_bench9.py, ngram_bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,\n            ngram_bench9.py, ngram_bench1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,\n            ngram_bench9.py, ngram_bench10.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,\n            ngram_bench9.py, ngram_bench10.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,\n            ngram_bench9.py, ngram_bench10.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,\n            ngram_bench9.py, ngram_bench10.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,\n            ngram_bench9.py, ngram_bench10.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,\n            ngram_bench9.py, ngram_bench10.py, ngram_b.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,\n            ngram_bench9.py, ngram_bench10.py, ngram_bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,\n            ngram_bench9.py, ngram_bench10.py, ngram_bench1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,\n            ngram_bench9.py, ngram_bench10.py, ngram_bench11.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,\n            ngram_bench9.py, ngram_bench10.py, ngram_bench11.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,\n            ngram_bench9.py, ngram_bench10.py, ngram_bench11.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,\n            ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,\n            ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,\n            ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_b.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,\n            ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,\n            ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,\n            ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,\n            ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,\n            ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,\n            ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,\n            ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,\n            ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py, ngram_b.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,\n            ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py, ngram_bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,\n            ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py, ngram_bench1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,\n            ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py, ngram_bench13.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,\n            ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py, ngram_bench13.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,\n            ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py, ngram_bench13.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,\n            ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py, ngram_bench13.py,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,\n            ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py, ngram_bench13.py,\n           .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,\n            ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py, ngram_bench13.py,\n            n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,\n            ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py, ngram_bench13.py,\n            ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,\n            ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py, ngram_bench13.py,\n            ngram_b.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,\n            ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py, ngram_bench13.py,\n            ngram_bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table lives on NVMe and is read through `mmap` + `MAP_POPULATE` + `MADV_WILLNEED` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-table-path` argument is a file path, not a directory: `ple/ple.f8_e4m3.bin` (CAMPAIGN.md:448).\nThe `--ple-ple-emb-norm-emb-path` argument is a directory: `ple/` (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 ring KV, int4 KV, PLE gather, PLE n-gram lookup\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_bench2.py, ngram_bench3.py,\n            ngram_bench4.py, ngram_bench5.py, ngram_bench6.py, ngram_bench7.py, ngram_bench8.py,\n            ngram_bench9.py, ngram_bench10.py, ngram_bench11.py, ngram_bench12.py, ngram_bench13.py,\n            ngram_bench1",
 "ttft_seconds": 1.1538599449995672,
 "status": "ok",
 "tg_tokens_per_s": 38.01005511272846,
 "decode_seconds_per_token": 0.02630882794129728,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 25.951989999157377,
  "p95": 32.942079997155815,
  "mean": 26.30882794129728
 },
 "class": "prose",
 "requested_input_tokens": 2048
}
