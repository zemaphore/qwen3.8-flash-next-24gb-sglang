{
 "started_utc": "2026-09-16T22:30:11.584974+00:00",
 "wall_seconds": 26.47352093399968,
 "prompt_tokens": 32768,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T22:30:38.058641+00:00",
 "warmup": false,
 "sample_index": 3,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 95,
  "temperature_c": {
   "min": 78.0,
   "max": 80.0,
   "mean": 79.6842105263158
  },
  "power_w": {
   "min": 263.23,
   "max": 279.7,
   "mean": 273.6066315789474
  },
  "util_pct": {
   "min": 88.0,
   "max": 99.0,
   "mean": 93.73684210526316
  },
  "clock_sm_mhz": {
   "min": 1395.0,
   "max": 1905.0,
   "mean": 1726.578947368421
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 1526.0
  }
 },
 "output_text_sha256": "010018430368f06198f015b64a9aa2706609399081476b19539da19edb63bae4",
 "output_text": " last last  last 1 last 16 last 16 tokens last 16 tokens of last 16 tokens of every last 16 tokens of every request last 16 tokens of every request, last 16 tokens of every request, so last 16 tokens of every request, so f last 16 tokens of every request, so f >= last 16 tokens of every request, so f >=  last 16 tokens of every request, so f >= 0 last 16 tokens of every request, so f >= 0. last 16 tokens of every request, so f >= 0.0 last 16 tokens of every request, so f >= 0.06 last 16 tokens of every request, so f >= 0.06; last 16 tokens of every request, so f >= 0.06; f last 16 tokens of every request, so f >= 0.06; f= last 16 tokens of every request, so f >= 0.06; f=0 last 16 tokens of every request, so f >= 0.06; f=0. last 16 tokens of every request, so f >= 0.06; f=0.2 last 16 tokens of every request, so f >= 0.06; f=0.25 last 16 tokens of every request, so f >= 0.06; f=0.25 gives last 16 tokens of every request, so f >= 0.06; f=0.25 gives  last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: -- last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-d last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g3 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 ( last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py: last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:6 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:69 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693- last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-6 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-69 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py: last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:2 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22- last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-7 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus S last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGL last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_K last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TI last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS= last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook ( last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_config last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py: last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:15 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:159 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, Hybrid last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinear last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKV last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool: last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:38 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:386 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863- last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-38 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-386 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867). last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\", last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \" last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \" last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model ( last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/s last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt( last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(1 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale =  last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2. last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS ( last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (me last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N( last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0, last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token =  last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1. last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 % last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g3 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 =  last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8. last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % ( last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (abs last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/ last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over  last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 3 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g3 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing =  last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8. last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels ( last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the  last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1. last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.2 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25- last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1. last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.4 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-s last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys ( last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same Ro last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf1 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 ( last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (7 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (76 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, q last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py: last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:3 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36- last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-5 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^ last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * ( last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~  last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0. last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 *  last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 1 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~  last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a Q last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~ last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2. last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax log last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~  last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0. last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.0 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(q last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~  last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0. last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.0 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 *  last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2. last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~  last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0. last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.1 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 n last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which pertur last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~ last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~1 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative ( last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). N last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added N last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on dem last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum p last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum p_i last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum p_i^ last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum p_i^2 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum p_i^2 over last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum p_i^2 over cold last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum p_i^2 over cold rows last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum p_i^2 over cold rows, last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum p_i^2 over cold rows, which last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum p_i^2 over cold rows, which is last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum p_i^2 over cold rows, which is un last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum p_i^2 over cold rows, which is unme last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum p_i^2 over cold rows, which is unmeasured last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum p_i^2 over cold rows, which is unmeasured here last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum p_i^2 over cold rows, which is unmeasured here; last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum p_i^2 over cold rows, which is unmeasured here; the last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum p_i^2 over cold rows, which is unmeasured here; the design last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum p_i^2 over cold rows, which is unmeasured here; the design's last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum p_i^2 over cold rows, which is unmeasured here; the design's own last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum p_i^2 over cold rows, which is unmeasured here; the design's own kill last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum p_i^2 over cold rows, which is unmeasured here; the design's own kill rule last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum p_i^2 over cold rows, which is unmeasured here; the design's own kill rule is last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum p_i^2 over cold rows, which is unmeasured here; the design's own kill rule is R last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum p_i^2 over cold rows, which is unmeasured here; the design's own kill rule is R(T last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum p_i^2 over cold rows, which is unmeasured here; the design's own kill rule is R(T) last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum p_i^2 over cold rows, which is unmeasured here; the design's own kill rule is R(T) > last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum p_i^2 over cold rows, which is unmeasured here; the design's own kill rule is R(T) >  last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum p_i^2 over cold rows, which is unmeasured here; the design's own kill rule is R(T) > 0 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum p_i^2 over cold rows, which is unmeasured here; the design's own kill rule is R(T) > 0. last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum p_i^2 over cold rows, which is unmeasured here; the design's own kill rule is R(T) > 0.0 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum p_i^2 over cold rows, which is unmeasured here; the design's own kill rule is R(T) > 0.05 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum p_i^2 over cold rows, which is unmeasured here; the design's own kill rule is R(T) > 0.05 on last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum p_i^2 over cold rows, which is unmeasured here; the design's own kill rule is R(T) > 0.05 on any last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum p_i^2 over cold rows, which is unmeasured here; the design's own kill rule is R(T) > 0.05 on any layer last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum p_i^2 over cold rows, which is unmeasured here; the design's own kill rule is R(T) > 0.05 on any layer. last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum p_i^2 over cold rows, which is unmeasured here; the design's own kill rule is R(T) > 0.05 on any layer. The last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum p_i^2 over cold rows, which is unmeasured here; the design's own kill rule is R(T) > 0.05 on any layer. The target last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum p_i^2 over cold rows, which is unmeasured here; the design's own kill rule is R(T) > 0.05 on any layer. The target ' last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum p_i^2 over cold rows, which is unmeasured here; the design's own kill rule is R(T) > 0.05 on any layer. The target 'ind last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum p_i^2 over cold rows, which is unmeasured here; the design's own kill rule is R(T) > 0.05 on any layer. The target 'indistinguish last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum p_i^2 over cold rows, which is unmeasured here; the design's own kill rule is R(T) > 0.05 on any layer. The target 'indistinguishable last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum p_i^2 over cold rows, which is unmeasured here; the design's own kill rule is R(T) > 0.05 on any layer. The target 'indistinguishable from last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum p_i^2 over cold rows, which is unmeasured here; the design's own kill rule is R(T) > 0.05 on any layer. The target 'indistinguishable from bf last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum p_i^2 over cold rows, which is unmeasured here; the design's own kill rule is R(T) > 0.05 on any layer. The target 'indistinguishable from bf1 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum p_i^2 over cold rows, which is unmeasured here; the design's own kill rule is R(T) > 0.05 on any layer. The target 'indistinguishable from bf16 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum p_i^2 over cold rows, which is unmeasured here; the design's own kill rule is R(T) > 0.05 on any layer. The target 'indistinguishable from bf16' last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum p_i^2 over cold rows, which is unmeasured here; the design's own kill rule is R(T) > 0.05 on any layer. The target 'indistinguishable from bf16' cannot last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8k tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 and kv_cache_dtype.py:22-75 -> torch.uint8) plus SGLANG_KV_TIERS=1; the pool class comes in through the existing full_kv_pool_class hook (kv_cache_configurator.py:1594, HybridLinearKVPool:3863-3867).\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, step s, RMS = s/sqrt(12)): fp8_e4m3 unit scale = 2.7 % relative RMS (measured on N(0,3)); int8 per-token = 1.3 %; int4-g32 = 8.6 % (absmax/7 over 32 channels); int4-g32 with static K smoothing = 8.6 % on the smoothed K channels (the smoothing removes the 1.25-1.48 channel-spread term, not the rounding term). K smoothing is exact for the indexer's own keys (same projection, same norm, same RoPE) and for the attention K after the norm; the indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53). Noise propagation: attention output error ~ sqrt(sum_i p_i^2) * (K noise + V noise) ~ 0.3 * 17 % ~ 5 % relative RMS on the output of a QSA layer for cold rows, vs ~2.5 % for fp8 today; softmax logit noise ~ 0.06 * std(qk) ~ 0.06 * 2.4 ~ 0.14 nats, which perturbs softmax weights by ~14 % relative (zero-mean, independent per key). NLL impact is second order in the noise: the added NLL is bounded by the mass of attention that lands on demoted rows, R(T) = sum p_i^2 over cold rows, which is unmeasured here; the design's own kill rule is R(T) > 0.05 on any layer. The target 'indistinguishable from bf16' cannot be",
 "ttft_seconds": 13.021604355002637,
 "status": "ok",
 "tg_tokens_per_s": 37.98734365442688,
 "decode_seconds_per_token": 0.026324557176123167,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 26.016550007625483,
  "p95": 32.908287990721874,
  "mean": 26.324557176123168
 },
 "class": "reasoning",
 "requested_input_tokens": 32768
}
