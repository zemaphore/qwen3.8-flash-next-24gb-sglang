{
 "started_utc": "2026-09-16T22:28:41.895164+00:00",
 "wall_seconds": 36.30410217499593,
 "prompt_tokens": 32768,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T22:29:18.199344+00:00",
 "warmup": true,
 "sample_index": 0,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 131,
  "temperature_c": {
   "min": 74.0,
   "max": 80.0,
   "mean": 77.45801526717557
  },
  "power_w": {
   "min": 155.4,
   "max": 283.15,
   "mean": 239.44847328244273
  },
  "util_pct": {
   "min": 0.0,
   "max": 99.0,
   "mean": 59.29770992366412
  },
  "clock_sm_mhz": {
   "min": 1365.0,
   "max": 1920.0,
   "mean": 1784.4274809160306
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 1526.0
  }
 },
 "output_text_sha256": "86fba317bd7a24d884afaee90d2fec7b1534af97b8ef08d8dec9ae3b3c963656",
 "output_text": " last last  last 1 last 16 last 16 tokens last 16 tokens of last 16 tokens of every last 16 tokens of every request last 16 tokens of every request, last 16 tokens of every request, so last 16 tokens of every request, so f last 16 tokens of every request, so f >= last 16 tokens of every request, so f >=  last 16 tokens of every request, so f >= 0 last 16 tokens of every request, so f >= 0. last 16 tokens of every request, so f >= 0.0 last 16 tokens of every request, so f >= 0.06 last 16 tokens of every request, so f >= 0.06; last 16 tokens of every request, so f >= 0.06; f last 16 tokens of every request, so f >= 0.06; f= last 16 tokens of every request, so f >= 0.06; f=0 last 16 tokens of every request, so f >= 0.06; f=0. last 16 tokens of every request, so f >= 0.06; f=0.2 last 16 tokens of every request, so f >= 0.06; f=0.25 last 16 tokens of every request, so f >= 0.06; f=0.25 gives last 16 tokens of every request, so f >= 0.06; f=0.25 gives  last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 81 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 819 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: -- last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-d last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g3 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 ( last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py: last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:6 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:69 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693- last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-6 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-69 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py: last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:2 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22- last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-7 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/pro last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/prof last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + S last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGL last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_K last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TI last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS= last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + S last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGL last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_K last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TI last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F= last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0. last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.2 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MH last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHAT last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHAToken last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenTo last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKV last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPool last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMX last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier ( last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode). last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\", last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \" last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \" last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/s last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt( last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(1 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N( last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0, last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured  last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2. last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token abs last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax  last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1. last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 % last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g3 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 ( last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (3 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, abs last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/ last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7)  last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8. last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 % last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g1 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16  last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 1 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11. last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight ( last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by  last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-R last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-e last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sig last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas ( last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (me last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread  last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1. last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.2 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25- last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1. last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.4 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g3 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~ last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8. last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~ last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1. last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing ->  last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8. last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^ last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * ( last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K log last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention ( last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^ last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~  last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0. last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~ last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0. last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.0 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.02 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 n last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of log last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added N last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by ( last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x  last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8. last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^ last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), un last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unme last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error ( last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2. last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 % last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP- last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured  last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 1 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16- last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-2 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is  last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp8 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp8. last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp8. The last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp8. The indexer last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp8. The indexer selects last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp8. The indexer selects by last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp8. The indexer selects by content last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp8. The indexer selects by content, last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp8. The indexer selects by content, not last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp8. The indexer selects by content, not age last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp8. The indexer selects by content, not age, last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp8. The indexer selects by content, not age, so last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp8. The indexer selects by content, not age, so ' last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp8. The indexer selects by content, not age, so 'cold last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp8. The indexer selects by content, not age, so 'cold' last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp8. The indexer selects by content, not age, so 'cold' pages last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp8. The indexer selects by content, not age, so 'cold' pages are last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp8. The indexer selects by content, not age, so 'cold' pages are still last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp8. The indexer selects by content, not age, so 'cold' pages are still attended last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp8. The indexer selects by content, not age, so 'cold' pages are still attended when last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp8. The indexer selects by content, not age, so 'cold' pages are still attended when relevant last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp8. The indexer selects by content, not age, so 'cold' pages are still attended when relevant; last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp8. The indexer selects by content, not age, so 'cold' pages are still attended when relevant; the last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp8. The indexer selects by content, not age, so 'cold' pages are still attended when relevant; the only last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp8. The indexer selects by content, not age, so 'cold' pages are still attended when relevant; the only guarantee last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp8. The indexer selects by content, not age, so 'cold' pages are still attended when relevant; the only guarantee is last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp8. The indexer selects by content, not age, so 'cold' pages are still attended when relevant; the only guarantee is that last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp8. The indexer selects by content, not age, so 'cold' pages are still attended when relevant; the only guarantee is that the last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp8. The indexer selects by content, not age, so 'cold' pages are still attended when relevant; the only guarantee is that the newest last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp8. The indexer selects by content, not age, so 'cold' pages are still attended when relevant; the only guarantee is that the newest chunk last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp8. The indexer selects by content, not age, so 'cold' pages are still attended when relevant; the only guarantee is that the newest chunk, last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp8. The indexer selects by content, not age, so 'cold' pages are still attended when relevant; the only guarantee is that the newest chunk, the last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp8. The indexer selects by content, not age, so 'cold' pages are still attended when relevant; the only guarantee is that the newest chunk, the sink last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp8. The indexer selects by content, not age, so 'cold' pages are still attended when relevant; the only guarantee is that the newest chunk, the sink and last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp8. The indexer selects by content, not age, so 'cold' pages are still attended when relevant; the only guarantee is that the newest chunk, the sink and the last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp8. The indexer selects by content, not age, so 'cold' pages are still attended when relevant; the only guarantee is that the newest chunk, the sink and the last last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp8. The indexer selects by content, not age, so 'cold' pages are still attended when relevant; the only guarantee is that the newest chunk, the sink and the last  last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp8. The indexer selects by content, not age, so 'cold' pages are still attended when relevant; the only guarantee is that the newest chunk, the sink and the last 1 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp8. The indexer selects by content, not age, so 'cold' pages are still attended when relevant; the only guarantee is that the newest chunk, the sink and the last 16 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp8. The indexer selects by content, not age, so 'cold' pages are still attended when relevant; the only guarantee is that the newest chunk, the sink and the last 16 tokens last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp8. The indexer selects by content, not age, so 'cold' pages are still attended when relevant; the only guarantee is that the newest chunk, the sink and the last 16 tokens of last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp8. The indexer selects by content, not age, so 'cold' pages are still attended when relevant; the only guarantee is that the newest chunk, the sink and the last 16 tokens of every last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp8. The indexer selects by content, not age, so 'cold' pages are still attended when relevant; the only guarantee is that the newest chunk, the sink and the last 16 tokens of every request last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp8. The indexer selects by content, not age, so 'cold' pages are still attended when relevant; the only guarantee is that the newest chunk, the sink and the last 16 tokens of every request stay last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp8. The indexer selects by content, not age, so 'cold' pages are still attended when relevant; the only guarantee is that the newest chunk, the sink and the last 16 tokens of every request stay fp last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp8. The indexer selects by content, not age, so 'cold' pages are still attended when relevant; the only guarantee is that the newest chunk, the sink and the last 16 tokens of every request stay fp8 last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp8. The indexer selects by content, not age, so 'cold' pages are still attended when relevant; the only guarantee is that the newest chunk, the sink and the last 16 tokens of every request stay fp8. last 16 tokens of every request, so f >= 0.06; f=0.25 gives 8192 tokens of slack. Selector: --kv-cache-dtype int4_g32 (new choice in server_args.py:693-696, mapped to torch.float8_e4m3fn in kv_cache_dtype.py:22-75 so all sizing/profiling stays valid) + SGLANG_KV_TIERS=1 + SGLANG_KV_TIERS_F=0.25; the pool class is MHATokenToKVPoolMXFP8 extended with the cold tier (same class as today's fp8 mode).\",\n  \"expected_accuracy\": \"Error model: uniform rounding noise, RMS = step/sqrt(12); e4m3 unit scale on N(0,3) measured 2.7 % relative RMS; int8 per-token absmax 1.3 %; int4-g32 (32 channels, absmax/7) 8.6 %; int4-g16 11.7 %. K is smoothed by the k_norm weight (1+w_c): after dividing by 1+w_c the rows are unit-RMS with near-equal channel sigmas (measured gamma spread 1.25-1.48), so int4-g32 K error is ~8.6 % on the smoothed values, ~1.5 % on the raw ones; V has no smoothing -> 8.6 % on cold V. Attention output error = sqrt(sum_i p_i^2) * (K logit noise + V noise): for peaked attention (sum p^2 ~ 0.3) a cold key contributes ~0.026 nats of logit noise; the added NLL is bounded by (cold revival mass) x 8.6 % x sqrt(sum p^2 over cold keys), unmeasured. The hot tier keeps exactly today's fp8 error (2.7 %), so SP-INT8's measured 16-21x lower error variance is what this design gives up; the int4 cold tier is 3x worse than fp8. The indexer selects by content, not age, so 'cold' pages are still attended when relevant; the only guarantee is that the newest chunk, the sink and the last 16 tokens of every request stay fp8. If",
 "ttft_seconds": 22.896855152997887,
 "status": "ok",
 "tg_tokens_per_s": 38.11387283321199,
 "decode_seconds_per_token": 0.02623716577887649,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 25.865193994832225,
  "p95": 32.4598379957024,
  "mean": 26.23716577887649
 },
 "class": "reasoning",
 "requested_input_tokens": 32768
}
