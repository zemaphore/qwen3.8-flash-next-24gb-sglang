{
 "started_utc": "2026-09-16T22:29:18.201980+00:00",
 "wall_seconds": 27.041258641009335,
 "prompt_tokens": 32768,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T22:29:45.243298+00:00",
 "warmup": false,
 "sample_index": 1,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 97,
  "temperature_c": {
   "min": 78.0,
   "max": 80.0,
   "mean": 79.08247422680412
  },
  "power_w": {
   "min": 257.8,
   "max": 282.84,
   "mean": 272.5541237113402
  },
  "util_pct": {
   "min": 88.0,
   "max": 99.0,
   "mean": 93.96907216494846
  },
  "clock_sm_mhz": {
   "min": 1185.0,
   "max": 1905.0,
   "mean": 1707.0618556701031
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 1526.0
  }
 },
 "output_text_sha256": "0a615c7d05cd2a3afb2775ebe9df7cc0cad5097a2bd67f34354839a1e825c431",
 "output_text": " last last  last 1 last 16 last 16 tokens last 16 tokens of last 16 tokens of every last 16 tokens of every request last 16 tokens of every request, last 16 tokens of every request, so last 16 tokens of every request, so f last 16 tokens of every request, so f >= last 16 tokens of every request, so f >=  last 16 tokens of every request, so f >= 0 last 16 tokens of every request, so f >= 0. last 16 tokens of every request, so f >= 0.0 last 16 tokens of every request, so f >= 0.06 last 16 tokens of every request, so f >= 0.06.\", last 16 tokens of every request, so f >= 0.06.\",\n last 16 tokens of every request, so f >= 0.06.\",\n  last 16 tokens of every request, so f >= 0.06.\",\n  \" last 16 tokens of every request, so f >= 0.06.\",\n  \"expected last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \" last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model ( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/s last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(1 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale =  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.6 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS ( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (me last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0, last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g3 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 =  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.5 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel ( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains,  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 40 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 409 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n:  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.01 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.015 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.03 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-R last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-e last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sig last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas ( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05- last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.1 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g3 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.5 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the log last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-no last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.02 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024- last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.03 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.04 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044- last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.05 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 ( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (log last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.6 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65- last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.2 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23):  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5- last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold:  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.5 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output ( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (20 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (204 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is un last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unme last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan ( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-gr last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-gran last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 1 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <=  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added N last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <=  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.00 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 n last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats ( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) =  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.2 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~ last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 n last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats ( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf1 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 ( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (7 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (76 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, q last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py: last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:3 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36- last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-5 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of pre last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact ( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier is last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier is rejected last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier is rejected: last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier is rejected:  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier is rejected: 8 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier is rejected: 8. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier is rejected: 8.6 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier is rejected: 8.6% last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier is rejected: 8.6% RMS last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier is rejected: 8.6% RMS is last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier is rejected: 8.6% RMS is  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier is rejected: 8.6% RMS is 3 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier is rejected: 8.6% RMS is 3x last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier is rejected: 8.6% RMS is 3x worse last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier is rejected: 8.6% RMS is 3x worse than last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier is rejected: 8.6% RMS is 3x worse than fp last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier is rejected: 8.6% RMS is 3x worse than fp8 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier is rejected: 8.6% RMS is 3x worse than fp8 and last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier is rejected: 8.6% RMS is 3x worse than fp8 and would last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier is rejected: 8.6% RMS is 3x worse than fp8 and would be last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier is rejected: 8.6% RMS is 3x worse than fp8 and would be visible last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier is rejected: 8.6% RMS is 3x worse than fp8 and would be visible.\", last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier is rejected: 8.6% RMS is 3x worse than fp8 and would be visible.\",\n last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier is rejected: 8.6% RMS is 3x worse than fp8 and would be visible.\",\n  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier is rejected: 8.6% RMS is 3x worse than fp8 and would be visible.\",\n  \" last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier is rejected: 8.6% RMS is 3x worse than fp8 and would be visible.\",\n  \"expected last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier is rejected: 8.6% RMS is 3x worse than fp8 and would be visible.\",\n  \"expected_cost last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier is rejected: 8.6% RMS is 3x worse than fp8 and would be visible.\",\n  \"expected_cost\": last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier is rejected: 8.6% RMS is 3x worse than fp8 and would be visible.\",\n  \"expected_cost\": \" last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier is rejected: 8.6% RMS is 3x worse than fp8 and would be visible.\",\n  \"expected_cost\": \"Memory last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier is rejected: 8.6% RMS is 3x worse than fp8 and would be visible.\",\n  \"expected_cost\": \"Memory: last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier is rejected: 8.6% RMS is 3x worse than fp8 and would be visible.\",\n  \"expected_cost\": \"Memory:  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier is rejected: 8.6% RMS is 3x worse than fp8 and would be visible.\",\n  \"expected_cost\": \"Memory: 8 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier is rejected: 8.6% RMS is 3x worse than fp8 and would be visible.\",\n  \"expected_cost\": \"Memory: 8. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier is rejected: 8.6% RMS is 3x worse than fp8 and would be visible.\",\n  \"expected_cost\": \"Memory: 8.1 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier is rejected: 8.6% RMS is 3x worse than fp8 and would be visible.\",\n  \"expected_cost\": \"Memory: 8.1 KB last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier is rejected: 8.6% RMS is 3x worse than fp8 and would be visible.\",\n  \"expected_cost\": \"Memory: 8.1 KB/token last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier is rejected: 8.6% RMS is 3x worse than fp8 and would be visible.\",\n  \"expected_cost\": \"Memory: 8.1 KB/token at last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier is rejected: 8.6% RMS is 3x worse than fp8 and would be visible.\",\n  \"expected_cost\": \"Memory: 8.1 KB/token at f last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier is rejected: 8.6% RMS is 3x worse than fp8 and would be visible.\",\n  \"expected_cost\": \"Memory: 8.1 KB/token at f= last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier is rejected: 8.6% RMS is 3x worse than fp8 and would be visible.\",\n  \"expected_cost\": \"Memory: 8.1 KB/token at f=0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier is rejected: 8.6% RMS is 3x worse than fp8 and would be visible.\",\n  \"expected_cost\": \"Memory: 8.1 KB/token at f=0. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier is rejected: 8.6% RMS is 3x worse than fp8 and would be visible.\",\n  \"expected_cost\": \"Memory: 8.1 KB/token at f=0.2 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier is rejected: 8.6% RMS is 3x worse than fp8 and would be visible.\",\n  \"expected_cost\": \"Memory: 8.1 KB/token at f=0.25 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier is rejected: 8.6% RMS is 3x worse than fp8 and would be visible.\",\n  \"expected_cost\": \"Memory: 8.1 KB/token at f=0.25 ( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier is rejected: 8.6% RMS is 3x worse than fp8 and would be visible.\",\n  \"expected_cost\": \"Memory: 8.1 KB/token at f=0.25 (1 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier is rejected: 8.6% RMS is 3x worse than fp8 and would be visible.\",\n  \"expected_cost\": \"Memory: 8.1 KB/token at f=0.25 (12 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 1.56% per channel (simulation on the real k_norm/q_norm gains, 4096 tokens, Gaussian n: 0.0156 RMS, max 0.031). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/min 1.05-1.17), so int4-g32 K error is 1.56% and the logit-noise std is 0.024-0.031 vs 0.044-0.056 for fp8 (logit std 1.65-2.23): 2.5-3.5x lower variance than the hot fp8 tier. V cold: 1.56% relative on the attention output (2048 selected keys, softmax logits std 2.4). The two-tier design's real accuracy question is the mass of revived cold tokens R(T), which is unmeasured; the design's own measurement plan (log the per-granule attention mass of the cold tier over 10k tokens) is the only way to bound it. If R(T) <= 0.05 the added NLL is <= 0.005 nats (target met); if R(T) = 0.25 it is ~0.02 nats (fails). The cold tier is strictly better than fp8 per token, so the accuracy risk is entirely the fraction of attention mass that lands on cold rows, not the code itself. The indexer's compressed keys stay bf16 (768 B/token, qsa_kv_pool.py:36-53), so selection itself is exact; the first chunk of prefill and the newest tokens are exact (hot tier). Int4 for the hot tier is rejected: 8.6% RMS is 3x worse than fp8 and would be visible.\",\n  \"expected_cost\": \"Memory: 8.1 KB/token at f=0.25 (12f",
 "ttft_seconds": 12.970771738007898,
 "status": "ok",
 "tg_tokens_per_s": 36.317297110373914,
 "decode_seconds_per_token": 0.027535088774939515,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 27.086757007054985,
  "p95": 35.17845898750238,
  "mean": 27.535088774939513
 },
 "class": "reasoning",
 "requested_input_tokens": 32768
}
