{
 "started_utc": "2026-09-16T22:22:26.982210+00:00",
 "wall_seconds": 15.911290688003646,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T22:22:42.893731+00:00",
 "warmup": false,
 "sample_index": 5,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 57,
  "temperature_c": {
   "min": 78.0,
   "max": 79.0,
   "mean": 78.3157894736842
  },
  "power_w": {
   "min": 258.29,
   "max": 279.81,
   "mean": 265.7731578947368
  },
  "util_pct": {
   "min": 89.0,
   "max": 100.0,
   "mean": 92.0701754385965
  },
  "clock_sm_mhz": {
   "min": 1635.0,
   "max": 1920.0,
   "mean": 1893.6842105263158
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2540.0
  }
 },
 "output_text_sha256": "d6b5753dc2e64277815d3699a7de29e40cfafdf6178ca1d1927984623e2cf5a3",
 "output_text": "++       +        b+        b_z+        b_zp+        b_zp_num+        b_zp_num =+        b_zp_num = +        b_zp_num = 1+        b_zp_num = 12+        b_zp_num = 128+        b_zp_num = 128\n+        b_zp_num = 128\n++        b_zp_num = 128\n+   +        b_zp_num = 128\n+    elif+        b_zp_num = 128\n+    elif has+        b_zp_num = 128\n+    elif has_z+        b_zp_num = 128\n+    elif has_zp+        b_zp_num = 128\n+    elif has_zp and+        b_zp_num = 128\n+    elif has_zp and use+        b_zp_num = 128\n+    elif has_zp and use_int+        b_zp_num = 128\n+    elif has_zp and use_int2+        b_zp_num = 128\n+    elif has_zp and use_int2_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_sh+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_exp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr +=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_exp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N),+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float3+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] &+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] <+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K -+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(com+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # ------------------------------------------------+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] <+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K //+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 -+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k //+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(t+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint3+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bit+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >>+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_sh+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) &+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(com+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator +=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b -+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+                b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+                b =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+                b = b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+                b = b *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+                b = b * b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+                b = b * b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+                b = b * b_scale_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+                b = b * b_scale_ptr[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+                b = b * b_scale_ptr[None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+                b = b * b_scale_ptr[None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+                b = b * b_scale_ptr[None, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+                b = b * b_scale_ptr[None, None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+                b = b * b_scale_ptr[None, None, :]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+                b = b * b_scale_ptr[None, None, :]\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+                b = b * b_scale_ptr[None, None, :]\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+                b = b * b_scale_ptr[None, None, :]\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+                b = b * b_scale_ptr[None, None, :]\n+                b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+                b = b * b_scale_ptr[None, None, :]\n+                b =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+                b = b * b_scale_ptr[None, None, :]\n+                b = b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+                b = b * b_scale_ptr[None, None, :]\n+                b = b.reshape+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+                b = b * b_scale_ptr[None, None, :]\n+                b = b.reshape(B+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+                b = b * b_scale_ptr[None, None, :]\n+                b = b.reshape(BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+                b = b * b_scale_ptr[None, None, :]\n+                b = b.reshape(BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+                b = b * b_scale_ptr[None, None, :]\n+                b = b.reshape(BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+                b = b * b_scale_ptr[None, None, :]\n+                b = b.reshape(BLOCK_SIZE_K,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+                b = b * b_scale_ptr[None, None, :]\n+                b = b.reshape(BLOCK_SIZE_K, BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+                b = b * b_scale_ptr[None, None, :]\n+                b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+                b = b * b_scale_ptr[None, None, :]\n+                b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+                b = b * b_scale_ptr[None, None, :]\n+                b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+                b = b * b_scale_ptr[None, None, :]\n+                b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+                b = b * b_scale_ptr[None, None, :]\n+                b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+                b = b * b_scale_ptr[None, None, :]\n+                b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+                b = b * b_scale_ptr[None, None, :]\n+                b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+                accumulator+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+                b = b * b_scale_ptr[None, None, :]\n+                b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+                accumulator +=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+                b = b * b_scale_ptr[None, None, :]\n+                b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+                accumulator += tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+                b = b * b_scale_ptr[None, None, :]\n+                b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+                accumulator += tl.dot+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+                b = b * b_scale_ptr[None, None, :]\n+                b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+                accumulator += tl.dot(a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+                b = b * b_scale_ptr[None, None, :]\n+                b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+                accumulator += tl.dot(a,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+                b = b * b_scale_ptr[None, None, :]\n+                b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+                accumulator += tl.dot(a, b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+                b = b * b_scale_ptr[None, None, :]\n+                b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+                accumulator += tl.dot(a, b)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+                b = b * b_scale_ptr[None, None, :]\n+                b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+                accumulator += tl.dot(a, b)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+                b = b * b_scale_ptr[None, None, :]\n+                b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+                accumulator += tl.dot(a, b)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+                b = b * b_scale_ptr[None, None, :]\n+                b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+                accumulator += tl.dot(a, b)\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+                b = b * b_scale_ptr[None, None, :]\n+                b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+                accumulator += tl.dot(a, b)\n+        elif+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+                b = b * b_scale_ptr[None, None, :]\n+                b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+                accumulator += tl.dot(a, b)\n+        elif use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+                b = b * b_scale_ptr[None, None, :]\n+                b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+                accumulator += tl.dot(a, b)\n+        elif use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token[token_mask] >= num_valid_tokens:\n+            # the access of tokens is out of range in k out of bounds\n+            a = 0\n+        else:\n+            a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+            a = a.to(compute_type)\n+        # -----------------------------------------------------------\n+        # Calculate and accumulate\n+        if use_int2_w2a16:\n+            words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            words = words.to(tl.uint32, bitcast=True)\n+            b = (words >> b_shifter) & 3\n+            b = b.to(compute_type)\n+            if use_int8_w8a16:\n+                accumulator += tl.dot(a, b)\n+            else:\n+                b = b - b_zp_num\n+                b = b * b_scale_ptr[None, None, :]\n+                b = b.reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+                accumulator += tl.dot(a, b)\n+        elif use_int4",
 "ttft_seconds": 1.1761385980062187,
 "status": "ok",
 "tg_tokens_per_s": 34.679132142188635,
 "decode_seconds_per_token": 0.028835785045020133,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 28.828244991018437,
  "p95": 34.803816000930965,
  "mean": 28.83578504502013
 },
 "class": "code",
 "requested_input_tokens": 2048
}
