{
  "boot_label": "tg0_boot1",
  "cells": {
    "prose-2048": {
      "class": "prose",
      "requested_input_tokens": 2048,
      "actual_prompt_tokens": 2048,
      "n_measured_ok": 5,
      "n_samples": 6,
      "tg_tokens_per_s": {
        "values": [
          38.19494947225885,
          38.01005511272846,
          37.7225473534927,
          37.50270899000646,
          37.12946149456771
        ],
        "mean": 37.71194448461084,
        "sd": 0.3755907643726048
      },
      "per_request_throughput_tokens_per_s": [
        38.19494947225885,
        38.01005511272846,
        37.7225473534927,
        37.50270899000646,
        37.12946149456771
      ],
      "median_token_latency_ms": {
        "values": [
          25.825864999205805,
          25.951989999157377,
          25.98433700040914,
          26.581938000163063,
          26.38430100341793
        ],
        "median_of_medians": 25.98433700040914
      },
      "p95_token_latency_ms": {
        "values": [
          32.85262800636701,
          32.942079997155815,
          33.40402300818823,
          33.0604609916918,
          33.629016994382255
        ],
        "median_of_p95": 33.0604609916918
      },
      "individual_token_timing": [
        true,
        true,
        true,
        true,
        true
      ],
      "ttft_seconds": [
        1.4493229830113705,
        1.1538599449995672,
        1.1030381729942746,
        1.1136447889875853,
        1.0539004510064842
      ]
    },
    "reasoning-2048": {
      "class": "reasoning",
      "requested_input_tokens": 2048,
      "actual_prompt_tokens": 2048,
      "n_measured_ok": 5,
      "n_samples": 6,
      "tg_tokens_per_s": {
        "values": [
          40.229267815972726,
          39.884660502220356,
          39.14030731120801,
          39.27637518117546,
          39.11825971272801
        ],
        "mean": 39.529774104660916,
        "sd": 0.4473198631251399
      },
      "per_request_throughput_tokens_per_s": [
        40.229267815972726,
        39.884660502220356,
        39.14030731120801,
        39.27637518117546,
        39.11825971272801
      ],
      "median_token_latency_ms": {
        "values": [
          24.20521700696554,
          24.25592500367202,
          24.711042002309114,
          24.853274997440167,
          24.80607299366966
        ],
        "median_of_medians": 24.711042002309114
      },
      "p95_token_latency_ms": {
        "values": [
          32.906941007240675,
          32.60538300673943,
          33.56814400467556,
          33.41512799670454,
          34.40928000782151
        ],
        "median_of_p95": 33.41512799670454
      },
      "individual_token_timing": [
        true,
        true,
        true,
        true,
        true
      ],
      "ttft_seconds": [
        1.0757057660084683,
        1.1108862139953999,
        1.0601946230017347,
        1.149228703987319,
        1.1757061100070132
      ]
    },
    "code-2048": {
      "class": "code",
      "requested_input_tokens": 2048,
      "actual_prompt_tokens": 2048,
      "n_measured_ok": 5,
      "n_samples": 6,
      "tg_tokens_per_s": {
        "values": [
          36.979950323604086,
          34.270090508339955,
          35.61966269526113,
          36.28848582368229,
          34.679132142188635
        ],
        "mean": 35.56746429861522,
        "sd": 0.999002127609947
      },
      "per_request_throughput_tokens_per_s": [
        36.979950323604086,
        34.270090508339955,
        35.61966269526113,
        36.28848582368229,
        34.679132142188635
      ],
      "median_token_latency_ms": {
        "values": [
          26.737082996987738,
          28.9836390002165,
          27.571662998525426,
          27.130143993417732,
          28.828244991018437
        ],
        "median_of_medians": 27.571662998525426
      },
      "p95_token_latency_ms": {
        "values": [
          33.81630900548771,
          36.63389501161873,
          36.36851800547447,
          35.45770399796311,
          34.803816000930965
        ],
        "median_of_p95": 35.45770399796311
      },
      "individual_token_timing": [
        true,
        true,
        true,
        true,
        true
      ],
      "ttft_seconds": [
        1.2493816170026548,
        1.1909709549945546,
        1.2122786340041785,
        1.1510431359929498,
        1.1761385980062187
      ]
    },
    "code-32768": {
      "class": "code",
      "requested_input_tokens": 32768,
      "actual_prompt_tokens": 32768,
      "n_measured_ok": 5,
      "n_samples": 6,
      "tg_tokens_per_s": {
        "values": [

      "n_measured_ok": 5,
      "n_samples": 6,
      "tg_tokens_per_s": {
        "values": [
          29.17673329747139,
          31.10210283439226,
          30.196727646895127,
          30.793732532379163,
          31.482916342943163
        ],
        "mean": 30.55044253081622,
        "sd": 0.8056323978824101
      },
      "per_request_throughput_tokens_per_s": [
        29.17673329747139,
        31.10210283439226,
        30.196727646895127,
        30.793732532379163,
        31.482916342943163
      ],
      "median_token_latency_ms": {
        "values": [
          33.957405990804546,
          31.489430999499746,
          32.79638300591614,
          31.871931001660414,
          31.057041996973567
        ],
        "median_of_medians": 31.871931001660414
      },
      "p95_token_latency_ms": {
        "values": [
          42.73109100176953,
          42.80349200416822,
          43.38529300002847,
          43.7058309908025,
          42.04245599976275
        ],
        "median_of_p95": 42.80349200416822
      },
      "individual_token_timing": [
        true,
        true,
        true,
        true,
        true
      ],
      "ttft_seconds": [
        78.83208052700502,
        79.29070347000379,
        78.79587112700392,
        78.78443288699782,
        78.85118813900044
      ]
    },
    "prose-131072": {
      "class": "prose",
      "requested_input_tokens": 131072,
      "actual_prompt_tokens": 131072,
      "n_measured_ok": 5,
      "n_samples": 6,
      "tg_tokens_per_s": {
        "values": [
          30.143686740399048,
          29.986089010841102,
          29.817650354146842,
          28.05886793388585,
          23.772710931474105
        ],
        "mean": 28.355800994149387,
        "sd": 2.411815157726921
      },
      "per_request_throughput_tokens_per_s": [
        30.143686740399048,
        29.986089010841102,
        29.817650354146842,
        28.05886793388585,
        23.772710931474105
      ],
      "median_token_latency_ms": {
        "values": [
          33.16833400458563,
          33.11909300100524,
          33.727703004842624,
          34.533864993136376,
          37.95400900708046
        ],
        "median_of_medians": 33.727703004842624
      },
      "p95_token_latency_ms": {
        "values": [
          39.88249599933624,
          41.61723199649714,
          40.317862993106246,
          47.819245999562554,
          71.3282039941987
        ],
        "median_of_p95": 41.61723199649714
      },
      "individual_token_timing": [
        true,
        true,
        true,
        true,
        true
      ],
      "ttft_seconds": [
        82.45432768600585,
        82.56862076499965,
        82.24906977900537,
        82.71447845900548,
        125.13732094300212
      ]
    }
  },
  "capacity": {
    "started_utc": "2026-09-16T23:02:13.279615+00:00",
    "wall_seconds": 222.5436240019917,
    "prompt_tokens": 257456,
    "completion_tokens": 512,
    "stream_events": 512,
    "ended_utc": "2026-09-16T23:05:55.823380+00:00",
    "warmup": false,
    "sample_index": 0,
    "requested_decode_tokens": 512,
    "gpu": {
      "n": 768,
      "temperature_c": {
        "min": 72.0,
        "max": 78.0,
        "mean": 75.44661458333333
      },
      "power_w": {
        "min": 149.56,
        "max": 282.05,
        "mean": 222.99921874999998
      },
      "util_pct": {
        "min": 0.0,
        "max": 100.0,
        "mean": 61.1875
      },
      "clock_sm_mhz": {
        "min": 1365.0,
        "max": 1920.0,
        "mean": 1735.9375
      },
      "clock_mem_mhz": {
        "min": 9501.0,
        "max": 9501.0,
        "mean": 9501.0
      },
      "memory_free_mib": {
        "min": 8.0
      }
    },
    "output_text_sha256": "2bcd14e82950654fcdc57949c8b67542d40d17ceaa06bd176b9aebfc5ce2a0d8",
    "output_text": " # # build # build the # build the new # build the new directory # build the new directory (~ # build the new directory (~4 # build the new directory (~46 # build the new directory (~46 GB # build the new directory (~46 GB, # build the new directory (~46 GB, ~ # build the new directory (~46 GB, ~1 # build the new directory (~46 GB, ~1 h # build the new directory (~46 GB, ~1 h, # build the new directory (~46 GB, ~1 h, CPU # build the new directory (~46 GB, ~1 h, CPU only # build the new directory (~46 GB, ~1 h, CPU only) # build the new directory (~46 GB, ~1 h, CPU only)\n\n # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-b # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf1 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming, # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\n # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (<  # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB). # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\" # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\n # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\n # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\n # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom saf # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safet # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\n # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom saf # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safet # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.t # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\n # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEA # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expand # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\" # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEA # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/ # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/se # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g1 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g12 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\")) # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed Auto # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\n # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expand # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\" # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/ # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-b # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf1 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\")) # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf1 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\n # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS =  # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 1 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 12 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\n # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nD # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile( # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\" # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^( # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\. # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\. # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\ # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\ # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\ # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.( # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_at # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\. # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj| # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_at # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\ # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.( # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q| # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k| # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v| # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_ # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\ # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\. # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\" # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"| # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\. # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$ # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\" # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n) # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ---------------------------------------------------------------- # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\n # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_g # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gpt # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor): # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\" # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf1 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [ # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> ( # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (q # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int3 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [ # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/ # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], q # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int3 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [ # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/g # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/ # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f1 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [ # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/g # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]). # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_g # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gpt # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point  # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 1 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 12 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as  # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 1 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 12 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127;  # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int3 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    q # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for q # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros ( # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-end # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift  # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*( # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i% # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)). # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\" # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS ==  # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N %  # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 ==  # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, ( # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K) # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float(). # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.cont # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous() # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [ # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N] # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N) # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs(). # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim= # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) /  # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 1 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 12 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127. # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [ # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/g # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N] # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale ==  # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale, # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale,  # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale, 1 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale, 1e # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale, 1e- # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale, 1e-8 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale, 1e-8), # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale, 1e-8), scale # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale, 1e-8), scale) # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale, 1e-8), scale)\n # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale, 1e-8), scale)\n    # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale, 1e-8), scale)\n    q # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale, 1e-8), scale)\n    q = # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale, 1e-8), scale)\n    q = torch # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale, 1e-8), scale)\n    q = torch.round # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale, 1e-8), scale)\n    q = torch.round(g # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale, 1e-8), scale)\n    q = torch.round(g / # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale, 1e-8), scale)\n    q = torch.round(g / scale # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale, 1e-8), scale)\n    q = torch.round(g / scale[:, # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale, 1e-8), scale)\n    q = torch.round(g / scale[:, None # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale, 1e-8), scale)\n    q = torch.round(g / scale[:, None, # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale, 1e-8), scale)\n    q = torch.round(g / scale[:, None, : # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale, 1e-8), scale)\n    q = torch.round(g / scale[:, None, :]) # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale, 1e-8), scale)\n    q = torch.round(g / scale[:, None, :]) + # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale, 1e-8), scale)\n    q = torch.round(g / scale[:, None, :]) +  # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale, 1e-8), scale)\n    q = torch.round(g / scale[:, None, :]) + 1 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale, 1e-8), scale)\n    q = torch.round(g / scale[:, None, :]) + 12 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale, 1e-8), scale)\n    q = torch.round(g / scale[:, None, :]) + 128 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale, 1e-8), scale)\n    q = torch.round(g / scale[:, None, :]) + 128. # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale, 1e-8), scale)\n    q = torch.round(g / scale[:, None, :]) + 128.0 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale, 1e-8), scale)\n    q = torch.round(g / scale[:, None, :]) + 128.0\n # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale, 1e-8), scale)\n    q = torch.round(g / scale[:, None, :]) + 128.0\n    # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale, 1e-8), scale)\n    q = torch.round(g / scale[:, None, :]) + 128.0\n    q # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale, 1e-8), scale)\n    q = torch.round(g / scale[:, None, :]) + 128.0\n    q = # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale, 1e-8), scale)\n    q = torch.round(g / scale[:, None, :]) + 128.0\n    q = q # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale, 1e-8), scale)\n    q = torch.round(g / scale[:, None, :]) + 128.0\n    q = q.cl # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale, 1e-8), scale)\n    q = torch.round(g / scale[:, None, :]) + 128.0\n    q = q.clamp # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale, 1e-8), scale)\n    q = torch.round(g / scale[:, None, :]) + 128.0\n    q = q.clamp( # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale, 1e-8), scale)\n    q = torch.round(g / scale[:, None, :]) + 128.0\n    q = q.clamp(0 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale, 1e-8), scale)\n    q = torch.round(g / scale[:, None, :]) + 128.0\n    q = q.clamp(0, # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale, 1e-8), scale)\n    q = torch.round(g / scale[:, None, :]) + 128.0\n    q = q.clamp(0,  # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale, 1e-8), scale)\n    q = torch.round(g / scale[:, None, :]) + 128.0\n    q = q.clamp(0, 2 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale, 1e-8), scale)\n    q = torch.round(g / scale[:, None, :]) + 128.0\n    q = q.clamp(0, 25 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale, 1e-8), scale)\n    q = torch.round(g / scale[:, None, :]) + 128.0\n    q = q.clamp(0, 255 # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale, 1e-8), scale)\n    q = torch.round(g / scale[:, None, :]) + 128.0\n    q = q.clamp(0, 255). # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale, 1e-8), scale)\n    q = torch.round(g / scale[:, None, :]) + 128.0\n    q = q.clamp(0, 255).to # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale, 1e-8), scale)\n    q = torch.round(g / scale[:, None, :]) + 128.0\n    q = q.clamp(0, 255).to(torch # build the new directory (~46 GB, ~1 h, CPU only)\n\nThe sealed directory is read only; src-bf16 is read only. Output shards are written streaming,\none input shard at a time, so host memory stays bounded (< 8 GB).\n\"\"\"\nimport argparse, json, os, re, shutil, sys, time\nimport torch\nfrom safetensors import safe_open\nfrom safetensors.torch import save_file\n\nSEALED = os.path.expanduser(os.environ.get(\"SEALED\", \"~/quant/sealed-w2g128\"))  # the sealed AutoRound output\nSRC = os.path.expanduser(os.environ.get(\"SRC\", \"~/quant/src-bf16\"))  # the bf16 source checkpoint\nGS = 128\nDENSE_RX = re.compile(\n    r\"^(model\\.language_model\\.layers\\.\\d+\\.(linear_attn\\.out_proj|self_attn\\.(q|k|v|o)_proj)\\.weight\"\n    r\"|lm_head\\.weight)$\"\n)\n\n\n# ----------------------------------------------------------------- packing\ndef pack_int8_gptq(w: torch.Tensor):\n    \"\"\"bf16 [N_out, K_in] -> (qweight int32 [K/4, N], qzeros int32 [K/gs, N/4], scales f16 [K/gs, N]).\n\n    auto_gptq layout, symmetric: zero point 128 stored as 127; 4 int8 per int32 along K for\n    qweight and along N for qzeros (little-endian byte order = shift 8*(i%4)).\n    \"\"\"\n    N, K = w.shape\n    assert K % GS == 0 and N % 4 == 0, (N, K)\n    wf = w.float().T.contiguous()                        # [K, N]\n    g = wf.view(K // GS, GS, N)\n    scale = g.abs().amax(dim=1) / 127.0                  # [K/gs, N]\n    scale = torch.where(scale == 0, torch.full_like(scale, 1e-8), scale)\n    q = torch.round(g / scale[:, None, :]) + 128.0\n    q = q.clamp(0, 255).to(torch.int",
    "ttft_seconds": 202.2006221289921,
    "status": "ok",
    "tg_tokens_per_s": 25.119294075134196,
    "decode_seconds_per_token": 0.039810035943243664,
    "individual_token_timing": true,
    "event_intervals_ms": {
      "n": 511,
      "median": 37.75246201257687,
      "p95": 58.93817399919499,
      "mean": 39.81003594324366
    },
    "class": "code",
    "requested_input_tokens": 257456
  },
  "ended_utc": "2026-09-16T23:05:55.827109+00:00",
  "frozen_input_state_after": {
    "label": "after",
    "captured_utc": "2026-09-16T23:05:55.827135+00:00",
    "gpu": {
      "pstate": "P2",
      "temperature_c": 78.0,
      "power_w": 255.56,
      "memory_used_mib": 23021.0,
      "memory_free_mib": 1104.0,
      "util_pct": 93.0,
      "clock_sm_mhz": 1920.0,
      "clock_mem_mhz": 9501.0,
      "clock_gr_mhz": 1920.0
    },
    "elastic_status": "time 22:18:07\nS_min 184 S_max 184 floor 184\narena_GB 11.062\nvram_free_GB 4.515\nmass_covered 0.4665\npool [128, 0, 0, 0] pin_used_MB 178\nS [184, 184, 184, 184, 184, 184, 184, 184, 184, 184, 184, 184, 184, 184, 184, 184, 184, 184, 184, 184, 184, 184, 184, 184, 184, 184, 184, 184, 184, 184, 184, 184, 184, 184, 184, 184, 184, 184, 184, 184, 184, 184, 184, 184, 184, 184, 184, 184]\n",
    "server_log_tail": "[2026-09-16 23:05:35] Using nearest MoE expert-count config E=432 for requested E=448 from /root/qwen3.8-flash-next-24gb-sglang/assets/moe_configs/configs/triton_3_7_1/E=432,N=160,device_name=NVIDIA_GeForce_RTX_3090,dtype=int2_w2a16_down.json.\n[2026-09-16 23:05:35] Using nearest MoE expert-count config E=432 for requested E=437 from /root/qwen3.8-flash-next-24gb-sglang/assets/moe_configs/configs/triton_3_7_1/E=432,N=160,device_name=NVIDIA_GeForce_RTX_3090,dtype=int2_w2a16.json.\n[2026-09-16 23:05:35] Using nearest MoE expert-count config E=432 for requested E=437 from /root/qwen3.8-flash-next-24gb-sglang/assets/moe_configs/configs/triton_3_7_1/E=432,N=160,device_name=NVIDIA_GeForce_RTX_3090,dtype=int2_w2a16_down.json.\n[2026-09-16 23:05:35] Using nearest MoE expert-count config E=432 for requested E=423 from /root/qwen3.8-flash-next-24gb-sglang/assets/moe_configs/configs/triton_3_7_1/E=432,N=160,device_name=NVIDIA_GeForce_RTX_3090,dtype=int2_w2a16.json.\n[2026-09-16 23:05:35] Using nearest MoE expert-count config E=432 for requested E=423 from /root/qwen3.8-flash-next-24gb-sglang/assets/moe_configs/configs/triton_3_7_1/E=432,N=160,device_name=NVIDIA_GeForce_RTX_3090,dtype=int2_w2a16_down.json.\n[2026-09-16 23:05:35] Using nearest MoE expert-count config E=432 for requested E=455 from /root/qwen3.8-flash-next-24gb-sglang/assets/moe_configs/configs/triton_3_7_1/E=432,N=160,device_name=NVIDIA_GeForce_RTX_3090,dtype=int2_w2a16.json.\n[2026-09-16 23:05:35] Using nearest MoE expert-count config E=432 for requested E=455 from /root/qwen3.8-flash-next-24gb-sglang/assets/moe_configs/configs/triton_3_7_1/E=432,N=160,device_name=NVIDIA_GeForce_RTX_3090,dtype=int2_w2a16_down.json.\n[2026-09-16 23:05:35] Prefill batch, #new-seq: 1, #new-token: 4016, #cached-token: 0, full token usage: 0.98, mamba usage: 1.00, #running-req: 0, #queue-req: 0, #pending-token: 0, cuda graph: False, input throughput (token/s): 1141.36\n[2026-09-16 23:05:37] Decode batch, #running-req: 1, #full token: 257536, full token usage: 0.98, mamba num: 1, mamba usage: 1.00, cuda graph: True, gen throughput (token/s): 0.20, #queue-req: 0\n[2026-09-16 23:05:38] Decode batch, #running-req: 1, #full token: 257536, full token usage: 0.98, mamba num: 1, mamba usage: 1.00, cuda graph: True, gen throughput (token/s): 21.86, #queue-req: 0\n[2026-09-16 23:05:40] Decode batch, #running-req: 1, #full token: 257600, full token usage: 0.98, mamba num: 1, mamba usage: 1.00, cuda graph: True, gen throughput (token/s): 22.21, #queue-req: 0\n[2026-09-16 23:05:42] Decode batch, #running-req: 1, #full token: 257664, full token usage: 0.98, mamba num: 1, mamba usage: 1.00, cuda graph: True, gen throughput (token/s): 21.76, #queue-req: 0\n[2026-09-16 23:05:44] Decode batch, #running-req: 1, #full token: 257664, full token usage: 0.98, mamba num: 1, mamba usage: 1.00, cuda graph: True, gen throughput (token/s): 21.17, #queue-req: 0\n[2026-09-16 23:05:46] Decode batch, #running-req: 1, #full token: 257728, full token usage: 0.98, mamba num: 1, mamba usage: 1.00, cuda graph: True, gen throughput (token/s): 21.88, #queue-req: 0\n[2026-09-16 23:05:47] Decode batch, #running-req: 1, #full token: 257792, full token usage: 0.98, mamba num: 1, mamba usage: 1.00, cuda graph: True, gen throughput (token/s): 27.91, #queue-req: 0\n[2026-09-16 23:05:49] Decode batch, #running-req: 1, #full token: 257792, full token usage: 0.98, mamba num: 1, mamba usage: 1.00, cuda graph: True, gen throughput (token/s): 29.36, #queue-req: 0\n[2026-09-16 23:05:50] Decode batch, #running-req: 1, #full token: 257856, full token usage: 0.98, mamba num: 1, mamba usage: 1.00, cuda graph: True, gen throughput (token/s): 31.05, #queue-req: 0\n[2026-09-16 23:05:51] Decode batch, #running-req: 1, #full token: 257856, full token usage: 0.98, mamba num: 1, mamba usage: 1.00, cuda graph: True, gen throughput (token/s): 25.33, #queue-req: 0\n[2026-09-16 23:05:53] Decode batch, #running-req: 1, #full token: 257920, full token usage: 0.98, mamba num: 1, mamba usage: 1.00, cuda graph: True, gen throughput (token/s): 29.58, #queue-req: 0\n[2026-09-16 23:05:54] Decode batch, #running-req: 1, #full token: 257984, full token usage: 0.98, mamba num: 1, mamba usage: 1.00, cuda graph: True, gen throughput (token/s): 28.66, #queue-req: 0"
  }
}
