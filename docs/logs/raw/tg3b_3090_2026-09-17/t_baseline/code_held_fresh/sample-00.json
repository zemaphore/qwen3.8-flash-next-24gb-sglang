{
 "started_utc": "2026-09-17T06:53:57.595234+00:00",
 "wall_seconds": 16.042170727989287,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T06:54:13.637698+00:00",
 "warmup": true,
 "sample_index": 0,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 58,
  "temperature_c": {
   "min": 50.0,
   "max": 64.0,
   "mean": 60.96551724137931
  },
  "power_w": {
   "min": 25.25,
   "max": 263.58,
   "mean": 231.32327586206895
  },
  "util_pct": {
   "min": 0.0,
   "max": 96.0,
   "mean": 78.98275862068965
  },
  "clock_sm_mhz": {
   "min": 1725.0,
   "max": 1965.0,
   "mean": 1925.6896551724137
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2540.0
  }
 },
 "output_text_sha256": "545fc4c1b1bd195620377da071112ccf6d4a2558fb7abd248322bb8fb3e9a5d8",
 "output_text": ",,\n,\n+,\n+                         ,\n+                          N,\n+                          N,,\n+                          N, H,\n+                          N, H,,\n+                          N, H, D,\n+                          N, H, D,,\n+                          N, H, D, ring,\n+                          N, H, D, ring_size,\n+                          N, H, D, ring_size,,\n+                          N, H, D, ring_size, sm,\n+                          N, H, D, ring_size, sm_scale,\n+                          N, H, D, ring_size, sm_scale,,\n+                          N, H, D, ring_size, sm_scale, sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift,\n+                          N, H, D, ring_size, sm_scale, sm_shift,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                         ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=3,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=6,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_war,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_st,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\",\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g3,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context +,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g6,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\",\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D %,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 3,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D %,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 6,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \",\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g3,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D %,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 3,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g6,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D %,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 6,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\",\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.b,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat1,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == r,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == r,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float1,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int6,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int6,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N *,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H *,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N *,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H *,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D //,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H *,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D //,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D //,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3) ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3) == ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3) == 1,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3) == 1\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3) == 1\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3) == 1\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3) == 1\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3) == 1\n+    assert k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3) == 1\n+    assert k_scale,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3) == 1\n+    assert k_scale.stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3) == 1\n+    assert k_scale.stride(,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3) == 1\n+    assert k_scale.stride(0,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3) == 1\n+    assert k_scale.stride(0),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3) == 1\n+    assert k_scale.stride(0) ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3) == 1\n+    assert k_scale.stride(0) == v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3) == 1\n+    assert k_scale.stride(0) == v_scale,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3) == 1\n+    assert k_scale.stride(0) == v_scale.stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3) == 1\n+    assert k_scale.stride(0) == v_scale.stride(,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3) == 1\n+    assert k_scale.stride(0) == v_scale.stride(0,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3) == 1\n+    assert k_scale.stride(0) == v_scale.stride(0),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3) == 1\n+    assert k_scale.stride(0) == v_scale.stride(0) ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3) == 1\n+    assert k_scale.stride(0) == v_scale.stride(0) == N,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3) == 1\n+    assert k_scale.stride(0) == v_scale.stride(0) == N *,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3) == 1\n+    assert k_scale.stride(0) == v_scale.stride(0) == N * H,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3) == 1\n+    assert k_scale.stride(0) == v_scale.stride(0) == N * H *,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3) == 1\n+    assert k_scale.stride(0) == v_scale.stride(0) == N * H * (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3) == 1\n+    assert k_scale.stride(0) == v_scale.stride(0) == N * H * (D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3) == 1\n+    assert k_scale.stride(0) == v_scale.stride(0) == N * H * (D //,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3) == 1\n+    assert k_scale.stride(0) == v_scale.stride(0) == N * H * (D // GROUP,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3) == 1\n+    assert k_scale.stride(0) == v_scale.stride(0) == N * H * (D // GROUP4,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3) == 1\n+    assert k_scale.stride(0) == v_scale.stride(0) == N * H * (D // GROUP4),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3) == 1\n+    assert k_scale.stride(0) == v_scale.stride(0) == N * H * (D // GROUP4)\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3) == 1\n+    assert k_scale.stride(0) == v_scale.stride(0) == N * H * (D // GROUP4)\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3) == 1\n+    assert k_scale.stride(0) == v_scale.stride(0) == N * H * (D // GROUP4)\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3) == 1\n+    assert k_scale.stride(0) == v_scale.stride(0) == N * H * (D // GROUP4)\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3) == 1\n+    assert k_scale.stride(0) == v_scale.stride(0) == N * H * (D // GROUP4)\n+    assert k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3) == 1\n+    assert k_scale.stride(0) == v_scale.stride(0) == N * H * (D // GROUP4)\n+    assert k_scale,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3) == 1\n+    assert k_scale.stride(0) == v_scale.stride(0) == N * H * (D // GROUP4)\n+    assert k_scale.stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3) == 1\n+    assert k_scale.stride(0) == v_scale.stride(0) == N * H * (D // GROUP4)\n+    assert k_scale.stride(,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3) == 1\n+    assert k_scale.stride(0) == v_scale.stride(0) == N * H * (D // GROUP4)\n+    assert k_scale.stride(1,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3) == 1\n+    assert k_scale.stride(0) == v_scale.stride(0) == N * H * (D // GROUP4)\n+    assert k_scale.stride(1),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3) == 1\n+    assert k_scale.stride(0) == v_scale.stride(0) == N * H * (D // GROUP4)\n+    assert k_scale.stride(1) ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3) == 1\n+    assert k_scale.stride(0) == v_scale.stride(0) == N * H * (D // GROUP4)\n+    assert k_scale.stride(1) == v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3) == 1\n+    assert k_scale.stride(0) == v_scale.stride(0) == N * H * (D // GROUP4)\n+    assert k_scale.stride(1) == v_scale,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3) == 1\n+    assert k_scale.stride(0) == v_scale.stride(0) == N * H * (D // GROUP4)\n+    assert k_scale.stride(1) == v_scale.stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3) == 1\n+    assert k_scale.stride(0) == v_scale.stride(0) == N * H * (D // GROUP4)\n+    assert k_scale.stride(1) == v_scale.stride(,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int4-g32 full-context + int8-g64 ring row).\n+    Returns the ring-owner stamp kernel (see _stamp_ring_owner) that MUST be launched before this kernel.\"\"\"\n+    assert D % 32 == 0 and D % 64 == 0, \"int4-g32 needs D % 32 == 0 and int8-g64 needs D % 64 == 0\"\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.bfloat16\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8\n+    assert loc.dtype == torch.int64 and owner.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert k.stride(2) == v.stride(2) == 1\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D and sm_k_inv.stride(1) == sm_v_inv.stride(1) == D\n+    assert sm_k_inv.stride(2) == sm_v_inv.stride(2) == 1\n+    assert k_buf.stride(0) == v_buf.stride(0) == N * H * (D // 2)\n+    assert k_buf.stride(1) == v_buf.stride(1) == H * (D // 2)\n+    assert k_buf.stride(2) == v_buf.stride(2) == (D // 2)\n+    assert k_buf.stride(3) == v_buf.stride(3) == 1\n+    assert k_scale.stride(0) == v_scale.stride(0) == N * H * (D // GROUP4)\n+    assert k_scale.stride(1) == v_scale.stride(1",
 "ttft_seconds": 2.631877808002173,
 "status": "ok",
 "tg_tokens_per_s": 38.105320265155825,
 "decode_seconds_per_token": 0.026243054592941382,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 25.861332993372343,
  "p95": 31.66257799603045,
  "mean": 26.24305459294138
 },
 "set": "code_held_fresh"
}
